# 결정 기록 (GR-0.2)

> **문서 5종** (D-0043 · D-0130) — 미래 `docs/PLAN.md` · 현재 `docs/DESIGN.md` ·
> 과거 `docs/DECISIONS.md` · 규약 `CONTRIBUTING.md` · 진입 `README.md`.
> **현재 문서: 과거.**

형식: 배경 / 후보 / 선택 / 근거 / 결과

**본문은 번호대별 조각에 있다** (O-30 · D-0080). 한 파일이 6000줄 또는 100건을
넘으면 `make split`이 다시 나눈다. **전체 색인은 이 문서 아래**에 있으며
`tools/sync_decision_index.py`가 조각 전부를 긁어 생성한다 (D-0133).

<!-- 이 목록은 tools/split_decisions.py가 생성한다. 손으로 고치지 않는다. -->

| 조각 | 범위 | 건수 |
|---|---|---|
| [`D-0001-0050.md`](decisions/D-0001-0050.md) | D-0001~D-0050 | 50건 |
| [`D-0051-0100.md`](decisions/D-0051-0100.md) | D-0051~D-0100 | 50건 |
| [`D-0101-0150.md`](decisions/D-0101-0150.md) | D-0101~D-0143 | 43건 |

## 색인

<!-- decision-index:begin -->
<!-- 이 표는 tools/sync_decision_index.py가 생성한다. 손으로 고치지 않는다 (D-0042). -->

| ID | 결정 |
|---|---|
| D-0001 | MongoDB를 core 프로파일에 유지한다 |
| D-0002 | 본인 보컬 시드 녹음을 P7까지 연기한다 |
| D-0003 | 실존 가수의 음색 복제·보간은 구현하지 않는다 |
| D-0004 | 저장소를 WSL ext4에 두고 F 드라이브는 데이터 전용으로 쓴다 |
| D-0005 | 가사는 태그 우선, STT 폴백 경로를 유지한다 |
| D-0006 | ISRC 부재로 Chromaprint 지문 정규화가 주 경로가 된다 |
| D-0007 | 샘플레이트는 인제스트에서 통일하지 않는다 |
| D-0008 | MASTER.md는 단일 진실 공급원이되 코드가 충돌 시 우선한다 |
| D-0009 | 노트북 2대 역할을 재정의하고 파이썬 3.12로 고정한다 |
| D-0010 | ID3 전수 조사 결과 유효 태그는 4종이며 태그 기반 메타데이터 경로를 폐기한다 |
| D-0011 | 취향은 선호도 예측이 아니라 조건부 생성으로 다룬다 |
| D-0012 | 취향 웜스타트는 적응적 쌍대비교로 수행한다 |
| D-0013 | 라이브러리 변경 추적은 명시적 재스캔 + 델타 감지로 한다 |
| D-0014 | 아티스트 파서는 정규화하지 않고 후보를 제시한다 |
| D-0015 | 로컬 에이전트 아키텍처를 채택하고 오디오 비이동을 불변 원칙으로 둔다 |
| D-0016 | 아티스트 파서 출력 스키마와 표기 체계 휴리스틱 |
| D-0017 | 정규화 기준을 ISRC에서 MusicBrainz MBID로 옮기고 Track을 폐기한다 |
| D-0018 | R-1 해소 — GPU 실동작 확인, MERT 청크 10초 확정 |
| D-0019 | MusicBrainz 조회는 2단계 구조로 하고 78.5%를 상한으로 받아들인다 |
| D-0020 | 재생시간으로 레코딩 버전을 확정한다 — O-5 해소, D-0019 정정 |
| D-0021 | 오디오 디코딩은 ffmpeg 서브프로세스, 정규화는 MERT 특징 추출기에 맡긴다 |
| D-0022 | 배치 중복 실행은 flock으로 막고 인덱스는 키 정렬로 유지한다 |
| D-0023 | 검색 평가 하네스를 태그 동치류로 구성한다 (M0/M1/M2) |
| D-0024 | MERT-95M 혼합 임베딩이 MFCC 베이스라인에 진다 — 실측 9조건 |
| D-0025 | MERT와 MFCC는 상보적이다 — O-8 가설 (c) 약화, (a)·(b)로 좁힌다 |
| D-0026 | MERT 마지막 레이어가 최악이었다 — layer00으로 교체, M2 2.8배 개선 |
| D-0027 | 레이어 곡선은 layer00에서 최대다 — O-9 해소, 임베딩 축 종료 |
| D-0028 | 취향 라벨 수집은 무작위 평가 집합을 먼저 확보한다 |
| D-0029 | 주기능은 시드곡 퓨전이다 — 취향 모델은 보조로 격하 |
| D-0030 | 임베딩 공간이 뭉쳐 허브 곡이 생긴다 — 중심화 도입 |
| D-0031 | 중심화가 전 지표를 개선한다 — 기본값으로 승격 |
| D-0032 | 중심화 후에도 레이어 순위가 유지된다 — layer00 확정 |
| D-0033 | 시드 결합 규칙을 실측으로 고른다 — M4 신설 |
| D-0034 | MEAN을 유지한다 — MIN은 균형을 개선하지 않았다. M4에 베이스라인 추가 |
| D-0035 | 퓨전은 작동하나 M4를 최적화 목표로 쓰지 않는다 — 퓨전 축 종료 |
| D-0036 | 가사축은 문자 n-gram 해싱 베이스라인으로 착수한다 |
| D-0037 | 가사축 M0 미달 — 진단 결과와 개선 축 3종 |
| D-0038 | 가사축 개선 3종 소진 — M0 정의가 가사에 맞지 않는다는 가설 |
| D-0039 | 문서 체계를 3축으로 명시한다 — 암묵 구조를 선언으로 전환 |
| D-0040 | M0 분할을 실험 축으로 승격한다 — 홀짝의 결정성이 가사에서 위험하다 |
| D-0041 | M0에 순위 진단과 해석적 베이스라인을 넣는다 |
| D-0042 | 부록 A를 손요약에서 기계 색인으로 바꾸고 문서 검사를 CI에 넣는다 |
| D-0043 | `PLAN.md`를 `DESIGN.md`로, GROUND RULES를 `CONTRIBUTING.md`로 — 4종 재분배 |
| D-0044 | O-12 판정 — 분할 정렬은 실재하나 작다. 가사 표현이 한계이며 신경망 인코더로 간다 |
| D-0045 | 가사축 인코더를 BGE-M3로 한다 — 혼재 40%가 결정 근거이며 판정표를 먼저 박는다 |
| D-0046 | BGE-M3 실측 — 방향은 맞았으나 판정 보류. 5배 기준에는 근거가 없었다 |
| D-0047 | CLI 배선을 테스트로 고정한다 — 인코더 테스트는 배선을 보지 않는다 |
| D-0048 | 가사축을 여기서 종료한다 — 풀링이 병목이 아니었다 |
| D-0049 | 구조 생성을 가사 반복 패턴에서 규칙으로 뽑는다 |
| D-0050 | 가사 구간 폴백 판정을 정리 뒤로 옮긴다 — 제외 1곡의 원인 확정 |
| D-0051 | 구조 추출의 절대 임계는 결과를 지배한다 — 상대 기준이 필요하다 |
| D-0052 | 생성 경로가 종단간 관통했다 — MIDI를 직접 쓴다 |
| D-0053 | n=1004 재추출 — 베이스라인이 축마다 달랐다 |
| D-0054 | 참조곡에서 조성을 추정한다 — 시드 퓨전이 구조에만 걸려 있었다 |
| D-0055 | 조성 추정 진단 — 격차 지표는 무작위와 구분되지 않는다 |
| D-0056 | 크로마를 반음 격자로 바꾼다 — 선형 FFT는 저역에서 반음을 못 가른다 |
| D-0057 | 반음 격자 실측 — 예측 하나가 틀렸고, 진단 지표의 분모가 틀렸다 |
| D-0058 | 로그 압축은 처방이 아니었다 — 평평함은 증상이지 원인이 아니다 |
| D-0059 | 베이스라인이 조건을 따라가지 않았다 — 판정이 뒤집혔다 |
| D-0060 | 베이스라인이 또 조건을 안 따라갔다 — 그리고 핵심 지표를 안 찍고 있었다 |
| D-0061 | O-23을 기각한다 — 쫓던 이상 자체가 베이스라인 없는 판정이었다 |
| D-0062 | O-21의 판정 장치를 생성기보다 먼저 만든다 — 조건화가 질 수 없는 지표를 피한다 |
| D-0063 | O-21을 닫는다 — 참조곡이 화성 어휘에 반영된다. 크기는 K-K 대비의 14%다 |
| D-0064 | 창별 중앙값 크로마를 넣는다 (O-27 (b)) — 그리고 `harmonic`이 조용히 무시되고 있었다 |
| D-0065 | O-27 (b) 기각 · 그리고 "귀무 λ\*는 0이어야 한다"가 틀린 기대였다 |
| D-0066 | 두 기기 개발 환경을 규약으로 고정한다 — 경로를 손으로 치지 않는다 |
| D-0067 | 규약을 기계가 확인하게 한다 — 규칙이 없어서가 아니라 확인하는 것이 없어서였다 |
| D-0068 | 기기 설정을 탐지해서 쓴다 — 탐지 가능한 것을 손으로 치게 했다 |
| D-0069 | 부류를 검사로 바꾼다 — 같은 결함이 네 시간 만에 재발했다 |
| D-0070 | 패치 적용과 커밋을 한 명령으로 묶는다 — 손으로 만든 명령이 매번 틀렸다 |
| D-0071 | 검사가 기기 상태에 의존했다 — 검사가 잡아야 할 부류를 검사 자신이 저질렀다 |
| D-0072 | 커밋 대상을 패치와 대조한다 — `git add -A`는 트리를 믿는 것이었다 |
| D-0073 | O-27 (a) 타악 분리를 구현한다 — 그리고 산출물에 조건을 적는다 |
| D-0074 | O-27 (a) 통과 — 드럼을 빼니 대비가 30%에서 87%로 올랐다. O-21을 다시 연다 |
| D-0075 | 조성 추출을 이어받게 한다 — 1004곡 배치가 원자적이었다 |
| D-0076 | (결번) 기록이 유실됐다 — 무엇이었는지 복원할 수 없다 |
| D-0077 | 추출을 동시에 못 돌게 한다 — 이어받기가 만든 결함이다 |
| D-0078 | 전량 배치를 마쳤다 — 출력 차이는 8마디로 판정할 수 없다 |
| D-0079 | O-29 도구를 만든다 — 8마디 실측은 극한보다 크고, 그 초과분은 정보가 아니다 |
| D-0080 | 결정 기록 자신의 규약을 기계가 검사하게 한다 — 78건이 쌓이는 동안 아무도 안 봤다 |
| D-0081 | 표기는 전수 강제하고 내용만 신규에 건다 — D-0080에서 둘을 뭉뚱그렸다 |
| D-0082 | O-29를 닫는다 — 전달된다. 그리고 "3배가 안 왔다"는 단위 착각이었다 |
| D-0083 | O-31 도구 — 버리는 칸의 몫은 나눗셈 하나로 갈린다. 모형이 필요 없었다 |
| D-0084 | 귀무선이 질량까지 바꿨다 — D-0062가 경고한 부류에 다시 빠졌다 |
| D-0085 | O-31을 닫는다 — 버린다. 그리고 버려지는 칸 중 하나는 반음계음이 아니었다 |
| D-0086 | 귀무선의 0점이 0이 아니었다 — 같은 자리에서 세 번째다 |
| D-0087 | 선을 하나 더했더니 다른 선의 수가 바뀌었다 — 귀무선 추출의 결함 |
| D-0088 | O-31을 닫는다 — 대등하다. 그리고 곡을 가르는 것은 이끔음이 아니었다 |
| D-0089 | O-33 도구 — 역인과를 상관으로 못 뚫으면 모양으로 뚫는다 |
| D-0090 | O-33을 닫는다 — 조성 오차가 아니다. 신뢰도로 나누자 정확히 갈렸다 |
| D-0091 | 차용은 뭉치고 누설은 고르게 번진다 — 그 대비가 둘을 가른다 |
| D-0092 | 머리글과 값을 따로 쓰다가 이름표가 밀렸다 — 실측을 한 번 그렇게 냈다 |
| D-0093 | O-35를 닫는다 — 단조 차용이 실재한다. 어휘 확장의 근거가 섰다 |
| D-0094 | 어휘를 넓힌다 — 그런데 넓히는 것만으로 거리가 오른다 |
| D-0095 | 어휘를 넓혔는데 번 몫이 없다 — 그리고 내 판정 규칙이 잡음으로도 이겼다 |
| D-0096 | 8마디에서 안 보인다고 없는 것이 아니다 — 되튐이 어휘 이득을 6배로 덮는다 |
| D-0097 | O-36을 닫는다 — 훑기가 예측대로다. 그리고 8은 마디 수가 아니었다 |
| D-0098 | 비싼 작업 전에 게이트를 세운다 — 순서 조건화는 표본으로 먼저 묻는다 |
| D-0099 | 게이트가 통과했는데 못 읽는다 — 두 관측이 오디오를 공유했다 |
| D-0100 | 이어받기가 조건을 다 보지 않았다 — 스템 조합을 더해도 "이미 처리했다" |
| D-0101 | O-32 게이트를 통과했다 — 겹침이 절반을 부풀리고 있었다 |
| D-0102 | 순서 지표를 먼저 만든다 — 재료보다 자가 먼저다 |
| D-0103 | 자기 전이를 빼면 구간 길이가 사라진다 — 박자 추정을 안 해도 된다 |
| D-0104 | 창 길이를 안 고른다 — 짧게 뽑아 두고 묶는다 |
| D-0105 | 크로마 시계열 추출기 — 기존 배치에 얹는다 |
| D-0106 | 시계열을 조합마다 뽑고 있었다 — 곡당 3분의 1이 낭비다 |
| D-0107 | 전이 사전 — 부드러운 집계를 써 보고 뒤집었다 |
| D-0108 | 한쪽 기기에서만 초록이었다 — 넘파이 판이 다르다 |
| D-0109 | 생성기가 배열을 조건화한다 — 순서 판정이 처음으로 참이 됐다 |
| D-0110 | 배열 사전이 생성 경로에 붙었다 — 처음으로 소리가 달라졌다 |
| D-0111 | 배열 조건화가 소리에 거의 안 닿고 있었다 — 한 이름이 두 값이었다 |
| D-0112 | 쌍 거리로는 배열 조건화를 판정할 수 없다 — self/other로 간다 |
| D-0113 | O-32를 닫는다 — 그리고 t가 열네 배 과대했다 |
| D-0114 | O-38을 닫는다 — 표본 부족이고, 부호는 쓰지 않는 지표에서만 뒤집힌다 |
| D-0115 | 결정 기록 하나가 조각 밖에 있었고 검사가 못 봤다 |
| D-0116 | 저장소 로더를 CLI에서 인프라로 내린다 |
| D-0117 | 파일 길이에 래칫을 건다 — 상한만으로는 되돌아간다 |
| D-0118 | 산출물 교두보를 세운다 — 그리고 래칫이 래칫이 아니었다 |
| D-0119 | 교두보가 실제 기기에서 세 자리에 걸렸다 — 왕복만 검사했다 |
| D-0120 | 쓰지 않는 것을 옮기다 죽었다 — DrvFs는 chmod를 못 한다 |
| D-0121 | 검사를 만들어 놓고 안 불렀다 — `--check`가 있는데 Makefile에 없다 |
| D-0122 | 광인사가 사라졌다 — 교두보가 백업이 됐고, OS를 두 단계 건너뛰었다 |
| D-0123 | 화성 리듬은 손잡이가 아니다 — 곡이 스스로 낸다 (O-37) |
| D-0124 | 유지 확률을 곡에서 뽑아 판정에 건다 — 배선만 하고 판정은 안 한다 |
| D-0125 | 유지 확률의 감소분은 전부 마디 수다 — O-37을 닫는다 |
| D-0126 | 닫힌 질문을 코드가 열린 것처럼 적는다 — 빚을 미루려다 크기를 안 쟀다 |
| D-0127 | `main.py`를 쪼개 래칫을 되돌린다 — 3955에서 3715로 |
| D-0128 | 규약이 "검사가 돈다"고 적었는데 그 검사가 없었다 |
| D-0129 | 문서 강제자 — 후보 다섯 중 넷을 실측으로 기각했다 |
| D-0130 | 문서 축을 시제로 다시 나눈다 — O-14를 열고 `PLAN.md`를 세운다 |
| D-0131 | 결정에 `강제자` 칸을 세운다 — 셋을 흡수하고 하나를 기각했다 |
| D-0132 | `강제자`를 전수 소급한다 — D-0080의 실수를 D-0131이 그대로 반복했다 |
| D-0133 | 빚 넷 중 셋을 한 번에 청산한다 — 그리고 인칭은 소급하지 않는다 |
| D-0134 | 오디오 비이동을 강제한다 — 서버가 없어도 구멍은 셀 수 있다 |
| D-0135 | 실측에 명령을 붙인다 — 재현되지 않는 기록을 하나 찾았다 |
| D-0136 | `재현`을 전수 소급한다 — 정의를 고르는 것으로 일을 없앴다 |
| D-0137 | 성부 진행을 넣는다 — 병행 5도 90.5%에서 17.5%로, 예측한 0%는 틀렸다 |
| D-0138 | 이어지는 같은 도수를 다시 치지 않는다 — 음길이 종류가 1에서 2로 |
| D-0139 | 베이스를 붙인다 — 음역 60~77에서 48~77로 |
| D-0140 | 처음으로 들었다 — 그냥 반주다 |
| D-0141 | 가락을 붙인다 — 새 값을 하나도 만들지 않는다 |
| D-0142 | 두 번째로 들었다 — 가락은 통했고 반주가 메트로놈이다 |
| D-0143 | 박과 온셋 — 실측에 걸기 전에 합성으로 떨어뜨려 봤다 |

<!-- decision-index:end -->

찾기:

```bash
grep -rn "D-0086" docs/decisions/
```

# 프로젝트 기획서

> **문서 4종** (D-0043) — 규약 `CONTRIBUTING.md` · 기획 `docs/DESIGN.md` ·
> 결정 `docs/DECISIONS.md` · 진입 `README.md`. **현재 문서: 기획.**


# HATHOR

## 취향 잠재 표현 기반 종단간 AI 음악 창작 시스템

사용자의 음악 취향을 기기 안에서 벡터로 만들고,
그 벡터를 조건으로 작사·작곡·편곡·가창까지 한 곡을 완성한다

2026. 08. 10.

1인 풀스택 취업 포트폴리오 | 저장소: github.com/CleverAIFox/hathor

---

**※ 데이터 경계 전제**: 사용자의 음원 파일과 원본 오디오는 **사용자 기기를 떠나지 않는다.** 분석은 로컬 에이전트가 수행하고 서버로는 취향 벡터와 특징 임베딩만
전송한다. 서버는 저작물을 애초에 보유하지 않는 구조로 설계한다. 이 원칙은 본 문서 전체를 관통하며, 어떤 기능도 이 경계를 넘지 않는다.

**※ 코퍼스 전제**: 개발용 코퍼스는 개발자 개인 보유 음원 1004곡(64.06시간, 9.29GB)이며 **배포 대상이 아니다.** 상용 음원이므로 서비스 코퍼스로 사용하지
않는다. 배포 시 참조가 필요한 경우 공개 라이선스 데이터셋(MTG-Jamendo, FMA)으로 교체하되, 로컬 에이전트 방식이므로 서비스 코퍼스 자체가 필수는 아니다.

**※ 수치 표기 원칙**: 본 문서의 모든 라이브러리 수치는 2026-08-10 실행한 인제스트 파이프라인 산출물에서 코드로 재산출한 값이다. 미검증 항목은 **(실측
필요)**로 명시하며 추측으로 채우지 않는다(GR-0.5).

**※ 문서 체계**: 3축이다 (D-0039). 본 문서는 **기획 축이자 단일 진실 공급원**으로 "무엇이 참인가"를 쓴다. `docs/DECISIONS.md`는 "왜 그것을
골랐고 무엇을 버렸는가"를 추가 전용으로 남긴다. `docs/archive/`는 폐기본이며 참조용이다.

**※ 명칭**: HATHOR는 이집트 신화에서 음악·춤·사랑을 관장한 여신 하토르에서 따왔다. 도구가 아니라 창작의 동반자를 지향한다는 의미를 담는다.

**※ 병행 과제 고지**: 개발자는 2026-12-11까지 광주 인공지능사관학교 7기 파이널 프로젝트(Fire-Lane, 5인 1조)를 병행한다. 해당 기간 HATHOR는
후순위이며, 일정 계획에 이 제약을 반영한다.

---

# Part I. 프로젝트 제안서

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| **프로젝트 주제** | **엣지 분석 기반 개인 취향 잠재 표현으로 조건화한 종단간 AI 음악 생성 시스템** |
| **개발 형태** | 1인 풀스택 (기획·설계·구현·배포·평가 전 과정)<br>기간: 2026-08 ~ 2027-02 (7개월, 병행 과제 고려)<br>예산: 개인 부담. GPU 인스턴스 상시 운영 제외, 보유 장비(GTX 1660Ti 6GB) 활용 |
| **개발자 및 역할** | 오창준 — 전 영역 단독 수행 |

### 추진배경 및 필요성

**○ [생성형 음악 서비스는 "누가 듣는가"를 모델링하지 않는다]**

Suno, Udio 등 현행 서비스는 텍스트 프롬프트를 입력으로 받는다. 프롬프트는 사용자의 **의도**를 전달하지만 **취향**은 전달하지 못한다. "슬픈 발라드"라고 입력한 두
사람이 원하는 결과는 전혀 다르며, 그 차이는 각자가 그동안 들어온 음악에 축적되어 있다. 현행 서비스는 이 축적된 정보를 입력 경로 자체에서 버린다.

**○ [사람은 자기 취향을 형용사로 표현하지 못한다]**

"어떤 음악을 좋아하세요"라는 질문에 정확히 답할 수 있는 사용자는 드물다. 반면 "이 곡 같은 느낌"이라는 지시는 누구나 할 수 있다. 취향 입력의 단위는 형용사가 아니라
**곡**이어야 한다. 이는 UI 편의의 문제가 아니라 정보량의 문제다. 곡 하나에는 형용사 열 개보다 많은 정보가 들어 있다.

**○ [취향 데이터는 개인 기기에 있고, 그것을 서버로 옮기는 순간 문제가 된다]**

사용자의 취향을 가장 잘 나타내는 자료는 그가 실제로 소유하고 듣는 음원이다. 그런데 이 음원은 대부분 저작권 보호 대상이다. 서비스가 이를 업로드받아 서버에 저장하는 순간 서비스
제공자가 복제 주체가 된다. 취향 정보를 얻으면서 저작물은 취득하지 않는 구조가 필요하다.

**○ [기존 시스템은 축이 분리되어 있지 않다]**

현행 생성 모델은 프롬프트 하나로 곡 전체를 만든다. "멜로디는 A곡처럼, 가창은 B곡처럼"과 같은 축별 지정이 불가능하다. 그러나 실제 창작에서 작곡·편곡·작사·가창은 분리된
작업이며, 사용자의 취향도 축마다 다르다. 발라드 멜로디를 좋아하면서 힙합 편곡을 선호하는 것은 모순이 아니다.

**○ [평가 기준이 "그럴듯함"에 머물러 있다]**

생성 음악의 품질 평가는 대부분 주관적 청취에 의존하거나 FAD 같은 분포 거리 지표에 머문다. "이 사용자의 취향에 맞는가"를 정량적으로 평가하는 체계가 없다. 취향을 벡터로
정의하면 생성 결과와 취향 벡터의 거리를 직접 측정할 수 있다.

### 목적 및 핵심가치

**○ [취향의 벡터화 — 프롬프트가 아니라 잠재 표현]**

사용자 라이브러리를 음악·편곡·가사·가창 4축으로 분석하여 각 축의 잠재 공간을 구축하고, 사용자의 취향을 그 공간 위의 벡터로 정의한다. 생성 시 이 벡터를 조건으로 사용한다.
프롬프트는 보조 입력이지 주 입력이 아니다.

**○ [엣지 우선 저작권 설계 — 서버는 음원을 보지 않는다]**

원본 오디오가 서버에 도달하는 순간 서비스가 복제 주체가 된다. 스캔·분석·특징 추출을 사용자 기기에서 수행하고, 서버로는 벡터만 전송한다. 이는 성능 최적화가 아니라 **법적
구조의 설계**다.

**○ [축별 독립 조건화 — "멜로디는 A, 가창은 B"]**

4축 각각에 다른 시드곡을 지정할 수 있게 한다. 프롬프트 기반 시스템이 구조적으로 제공할 수 없는 조작이며, 축을 분리해 설계한 것의 직접적 산출이다.

**○ [양성 사례만으로 성립하는 학습 설계]**

사용자 라이브러리는 전부 "좋아해서 소유한" 곡이다. 음성 사례가 존재하지 않는다. 이 문제를 외부 데이터로 메우는 대신, 문제를 선호도 예측에서 조건부 생성으로 재정의하여 양성
사례만으로 성립시킨다. 상대 순위 정보가 필요한 부분은 쌍대비교로 내부에서 생성한다.

**○ [실측이 설계를 결정한다 — 추측으로 채우지 않는다]**

라이브러리 태그 실측에서 발매일·ISRC가 전량 부재함을 확인하고 지문 기반 정규화를 주 경로로 승격했다. 아티스트 필드의 괄호가 피처링이 아니라 별칭 병기임을 확인하고 파서
설계를 뒤집었다. 미검증 항목은 (실측 필요)로 표시하고 추측으로 진행하지 않는다.

**○ [생성 결과의 안전 게이트 — 실존 가수 음색 차단]**

취향 학습이 특정 가수의 음색 복제로 귀결되면 인격권·퍼블리시티권 침해가 된다. 화자 검증(SV) 임베딩 코사인 유사도 임계를 두고, 생성 결과가 실존 가수와 식별 가능한 수준으로
유사하면 산출을 차단한다.

### 주요 서비스

**□ 서비스 메뉴 구조도**

**HATHOR System**

**1. 로컬 에이전트 (데스크톱 앱)**
- 1.1 라이브러리 스캔 및 태그 추출 (음원은 기기 외부로 전송하지 않음)
- 1.2 4축 특징 추출 파이프라인 (GPU 가속 / CPU 폴백)
- 1.3 백그라운드 분석 작업 — 중단·재개, 곡 단위 진행률
- 1.4 취향 벡터 산출 및 서버 동기화 (벡터만 전송)
- 1.5 로컬 분석 결과 조회 — 라이브러리 4축 프로파일 시각화

**2. 취향 프로파일링**
- 2.1 라이브러리 기반 초기 취향 벡터 자동 산출
- 2.2 적응적 쌍대비교 온보딩 (이상형 월드컵형, 20~30회)
- 2.3 축별 취향 분해 표시 (음악·편곡·가사·가창)
- 2.4 취향 변화 이력 및 재학습

**3. 곡 생성**
- 3.1 시드곡 선택 (축별 독립 지정 가능, 1~5곡)
- 3.2 미세조정 슬라이더 (템포·밝기·밀도 등, 시드 위의 보정)
- 3.3 생성 파이프라인 실행 — 구조 → 화성 → 멜로디 → 편곡 → 가사 → 가창 → 믹스
- 3.4 단계별 중간 산출물 열람 및 부분 재생성
- 3.5 시드 고정 재현 생성 (동일 시드 = 동일 산출물)

**4. 결과 관리**
- 4.1 생성 곡 라이브러리 및 버전 이력
- 4.2 스템 분리 다운로드 (보컬·드럼·베이스·기타)
- 4.3 MIDI·악보 내보내기
- 4.4 생성 결과 피드백 → 취향 벡터 갱신

**5. 품질 · 안전 게이트**
- 5.1 화자 동일성 검사 — 실존 가수 식별 가능 시 차단
- 5.2 멜로디 유사도 검사 — 기존 곡 표절 구간 탐지
- 5.3 취향 적합도 점수 — 생성 결과와 취향 벡터 거리
- 5.4 차단 사유 및 재생성 가이드 제시

**6. 관리 · 검증**
- 6.1 파이프라인 재실행 및 산출 이력(manifest) 조회
- 6.2 평가 리포트 — 베이스라인 대비 지표
- 6.3 데모 모드 — 설치 없이 사전 분석된 프로파일로 체험

### 주요 기술

**[Local Agent]**
- Tauri (Rust + 웹뷰). Electron 대비 번들 크기·메모리가 작고 웹 UI를 서버와 공유한다.
- 인제스트 파이프라인은 Python 사이드카 프로세스로 분리. 이미 구현된 스캐너·추출기를 그대로 재사용한다.
- 델타 감지(mtime + size)로 재분석을 변경분에 한정한다. 곡당 GPU 수십 초가 드는 스템 분리에서 이 절감이 결정적이다.
- drvfs·NTFS 등 파일시스템 감시가 불가능한 환경이 존재하므로 실시간 감시가 아니라 명시적 재스캔 방식을 채택한다.

**[Audio Analysis]**
- Demucs v4 (htdemucs) 스템 분리 — 편곡축·가창축의 전제. 44.1kHz 입력.
- MERT — 음악 이해 자기지도 표현. 음악축 임베딩. 24kHz 입력.
- CLAP — 오디오·텍스트 정렬 임베딩. 축 간 공통 참조 공간. 48kHz 입력.
- 샘플레이트는 인제스트에서 통일하지 않고 원본을 유지한 뒤 모델 입력 직전에 리샘플한다. 모델별 요구 샘플레이트가 상이하므로 사전 통일은 손실만 발생시킨다.
- Chromaprint 지문으로 곡 신원을 정규화한다. ISRC가 전량 부재하므로 지문이 주 경로다.

**[NLP · Lyrics]**
- USLT 태그 가사를 우선 사용하고, 부재 시 Whisper STT로 폴백한다. 포트를 분리해 구현체를 교체 가능하게 둔다.
- 동기 가사(SYLT)가 전량 부재하므로 한국어 강제정렬(Montreal Forced Aligner 등)로 시간 정보를 생성한다. 가창축 학습의 전제다.
- 가사 언어는 태그의 언어 필드를 신뢰하지 않고 본문에서 판별한다. 실측상 한국어 가사가 `eng`로 기재되어 있다.
- 한국어 사전학습 언어모델(KLUE-RoBERTa 계열)로 주제·정서 임베딩을 산출한다.

**[Generation]**
- 단계 분리 파이프라인: 구조 → 화성 → 멜로디 → 편곡 → 가사 → 가창 → 믹스. 각 단계가 독립 엔진이며 중간 산출물이 검사 가능하다.
- 심볼릭 단계(구조·화성·멜로디)는 MIDI로 다루고, 오디오 렌더링은 이후 단계에서 수행한다. 전 구간 오디오 생성보다 제어 가능성이 높다.
- 가창 합성은 화자 무의존 설계를 유지한다. 공개 다화자 데이터베이스를 베이스로 하고 취향 화자 가중치를 델타로 얹되, 단일 화자 기여 상한을 둔다.
- 전 파이프라인은 시드 고정 시 결정적으로 동작한다.

**[Backend & Infrastructure]**
- FastAPI (취향 동기화 API · 생성 작업 API · 결과 서빙)
- PostgreSQL + pgvector (취향 벡터·특징 임베딩 검색), MongoDB (생성 이력·중간 산출물), Redis (작업 상태), RabbitMQ (생성 작업
  큐), MinIO (산출물 오브젝트 스토리지)
- 계층 구조를 import-linter 계약으로 강제한다. 도메인 격리 / 애플리케이션 격리 / 엔진 간 직접 호출 금지 / 계층 순서 4종.
- CI 3잡: 품질(ruff·mypy strict·계약·커버리지) / 시크릿 스캔 / 재현성 검증.
- Docker Compose 기반 로컬 구성, 배포는 단일 인스턴스.

**[Frontend]**
- React(Vite) + TypeScript. 로컬 에이전트 웹뷰와 웹 대시보드가 동일 컴포넌트를 공유한다.
- 파형·스펙트로그램 표출은 WaveSurfer.js, 취향 공간 시각화는 D3 기반 2차원 투영(UMAP·t-SNE).

### 기대효과

**[취향 입력 방식의 대안 제시]**
- 텍스트 프롬프트가 아니라 시드곡과 잠재 벡터로 생성을 조건화하는 방식을 구현하고, 프롬프트 기반 베이스라인 대비 취향 적합도를 정량 비교한다.

**[저작물을 취득하지 않는 개인화 구조]**
- 원본 오디오가 서버에 도달하지 않으면서 개인 취향을 반영하는 구조를 실제 동작으로 제시한다. 음악뿐 아니라 개인 미디어 자료 전반에 적용 가능한 참조 설계가 된다.

**[축별 조건 분리의 실증]**
- "멜로디는 A곡, 가창은 B곡"과 같은 축별 지정이 실제로 결과에 반영되는지를 축별 절제 실험으로 검증한다.

**[생성 안전 게이트의 구현 사례]**
- 실존 가수 음색 복제를 화자 검증 임베딩 임계로 차단하는 구조를 구현하고 임계값을 실측으로 캘리브레이션한다. 정성적 정책 선언이 아니라 동작하는 게이트로 제시한다.

**[1인 풀스택 역량의 통합 증명]**
- 오디오 신호처리·심층학습·백엔드·데스크톱 앱·인프라·CI를 하나의 시스템에서 통합 운용한 이력을 남긴다. 개별 기술 나열이 아니라 상호 의존하는 시스템으로 증명한다.

### 활용방안 및 비즈니스 모델

**□ 활용 방안**

**[개인 창작 보조 도구]**
- 작곡 경험이 없는 사용자가 자신의 취향에 맞는 곡을 얻는다. 완성곡뿐 아니라 MIDI·스템을 제공하여 이후 편집의 출발점으로 쓸 수 있다.

**[창작자의 아이디어 스케치]**
- 작곡가가 자신의 기존 작업물을 시드로 넣어 유사 계열의 변주를 대량 생성하고 그중 발전시킬 소재를 고른다.

**[취향 분석 자체의 가치]**
- 생성을 사용하지 않더라도 자기 라이브러리의 4축 프로파일을 시각화하는 것만으로 사용자에게 정보 가치가 있다.

**□ 비즈니스 모델**

**[로컬 우선 프리미엄 구독]**
- 분석은 로컬에서 무료, 생성은 서버 GPU를 사용하므로 종량 또는 구독. 로컬 분석이 무료여도 서버 비용이 발생하지 않는 구조가 이 설계의 이점이다.

**[분석 모듈 라이선스]**
- 4축 특징 추출 파이프라인을 음악 서비스·DAW 플러그인에 공급하는 모듈형 라이선스.

**[한계 인식]**
- 본 프로젝트의 1차 목적은 수익이 아니라 취업 포트폴리오다. 비즈니스 모델은 설계 타당성을 검토하기 위한 항목이며 실제 사업화를 전제하지 않는다. 생성 음악의 저작권 귀속은
  국내외에서 정리되지 않았고, 이는 사업화 이전에 해소되어야 할 선결 조건이다.

---

## 2. 아이디어 도출 과정

본 프로젝트는 'AI 음악 생성'이라는 소재에서 출발했으나, 그것을 프롬프트가 아니라 취향 잠재 표현으로 다루기까지 네 단계의 발상 정리를 거쳤다.

### □ 강제연결법 (Forced Connection)

| 축 A — 하고 싶은 것 | 축 B — 보유 자산 | 결합 결과 |
|---|---|---|
| AI 음악 생성 | 프롬프트 엔지니어링 | 프롬프트 기반 생성 — Suno·Udio와 완전 중복. 차별성 없음 |
| AI 음악 생성 | 개인 음원 라이브러리 1004곡 | 취향 학습 기반 추천 — 추천은 생성이 아니며 Spotify와 중복 |
| 음악 추천 | 잠재 표현 학습 | 임베딩 기반 추천 시스템 — 다수 존재. 신규성 없음 |
| **AI 음악 생성 × 개인 라이브러리** | **잠재 표현 + 엣지 분석** | **개인 취향 벡터로 조건화한 생성 — 선행 사례 미확인. 채택** |

앞의 세 조합은 기존 서비스와 겹치거나 기술 신규성이 없었다. 네 번째만 오디오 분석·표현 학습·생성 모델이 모두 필수적으로 요구되며, 조사 결과 개인 라이브러리를 잠재 표현으로
변환해 생성을 조건화하는 상용 시스템이 확인되지 않았다.

### □ SCAMPER 적용

기준 대상은 '텍스트 프롬프트 기반 음악 생성 서비스'다.

| 기법 | 적용 | 결과 |
|---|---|---|
| **S**ubstitute | 프롬프트를 무엇으로 대체할 것인가 | 텍스트 → **시드곡 + 취향 벡터**. 사용자가 형용사를 고르지 않고 아는 곡을 고른다 |
| **C**ombine | 무엇과 결합할 것인가 | 생성 + **개인 라이브러리 분석**. 추천 시스템의 취향 모델링을 생성의 조건 입력으로 연결 |
| **A**dapt | 다른 분야에서 무엇을 가져올 것인가 | 연합학습·엣지 추론의 **데이터 비이동 원칙**을 저작권 문제에 적용 |
| **M**odify | 무엇을 확대·축소할 것인가 | 생성 단위를 곡 전체에서 **4축으로 분해**. 축별 독립 조건 지정 가능 |
| **P**ut to other use | 다른 용도 | 생성을 쓰지 않아도 **라이브러리 4축 프로파일 시각화** 자체가 산출물 |
| **E**liminate | 무엇을 제거할 것인가 | **음원 업로드를 제거**. 서버가 저작물을 보유하지 않음 |
| **R**everse | 무엇을 뒤집을 것인가 | "무엇을 좋아하는가"를 묻지 않고 **"둘 중 어느 쪽인가"를 반복**해 취향을 역산 (쌍대비교) |

### □ 모순 분석

설계 과정에서 정면으로 충돌한 요구를 정리한다.

| 모순 | 갈등 구조 | 해소 방식 |
|---|---|---|
| **개인화 vs 저작권** | 취향을 알려면 음원이 필요한데, 음원을 받으면 복제 주체가 된다 | 음원이 아니라 **음원에서 뽑은 벡터**만 이동시킨다. 분석을 엣지로 옮겨 문제를 발생 지점에서 제거 |
| **취향 학습 vs 음성 사례 부재** | 라이브러리는 전부 양성 사례이며 싫어하는 곡이 없다 | 문제를 **선호도 예측에서 조건부 생성으로 재정의**. 순위 정보가 필요한 부분은 쌍대비교로 내부 생성 |
| **취향 반영 vs 음색 도용** | 취향을 정확히 반영할수록 특정 가수 음색에 수렴한다 | 화자 무의존 베이스 + 가중치 델타. **단일 화자 기여 상한**과 SV 임베딩 게이트로 식별 가능한 산출만 차단 |
| **로컬 분석 vs 연산 자원** | 개인 기기에 GPU가 없을 수 있다 | CPU 폴백을 정식 구현하고 **백그라운드 중단·재개**로 흡수. 부분 결과로도 취향 벡터 산출 가능 |
| **결정적 재현 vs 생성 다양성** | 재현성 검증을 하려면 결정적이어야 하는데 창작은 다양해야 한다 | **시드를 명시적 입력으로 노출**. 동일 시드는 동일 산출, 시드 변경으로 다양성 확보 |

### □ 아이디어 평가

| 평가 항목 | 프롬프트 기반 생성 | 임베딩 기반 추천 | **취향 조건 생성 (채택)** |
|---|---|---|---|
| 기술 신규성 | 낮음 | 낮음 | **높음** |
| 1인 구현 가능성 | 높음 | 높음 | **중간** |
| 포트폴리오 서사 | 약함 | 중간 | **강함** |
| 법적 리스크 | 중간 | 낮음 | **중간 (설계로 완화)** |
| 데이터 확보 | 불필요 | 필요 | **보유 (1004곡)** |
| 평가 가능성 | 어려움 | 쉬움 | **가능 (벡터 거리)** |

채택안은 구현 난도가 가장 높으나, 나머지 항목에서 우위가 명확하고 데이터를 이미 보유하고 있다. 난도는 단계 분할(P0~P8)과 각 단계의 독립 검증으로 관리한다.

---

## 3. 시장현황 및 유사 서비스 분석

### □ 유사 서비스 분석 및 비교

| 서비스 | 입력 방식 | 개인화 | 축 분리 | 데이터 경계 | 산출물 |
|---|---|---|---|---|---|
| **Suno** | 텍스트 프롬프트 + 가사 | 없음 | 없음 | 서버 처리 | 완성 오디오 |
| **Udio** | 텍스트 프롬프트 | 없음 | 부분 (리믹스) | 서버 처리 | 완성 오디오 |
| **SoundRaw** | 장르·무드 선택 | 없음 | 부분 (파트 교체) | 서버 처리 | 완성 오디오 |
| **AIVA** | 스타일 프리셋 | 없음 | 있음 (MIDI 편집) | 서버 처리 | MIDI + 오디오 |
| **Mubert** | 텍스트·태그 | 없음 | 없음 | 서버 처리 | 스트림 |
| **MusicGen (Meta)** | 텍스트 + 멜로디 조건 | 없음 | 부분 (멜로디 조건) | 로컬 실행 가능 | 오디오 |
| **HATHOR** | **시드곡 + 취향 벡터** | **라이브러리 기반** | **4축 독립** | **엣지 분석, 음원 비이동** | **오디오 + 스템 + MIDI** |

**분석 요약**

- 개인 취향을 **누적 학습**하는 생성 서비스는 확인되지 않는다. 모든 서비스가 세션 단위 프롬프트로 동작한다.
- MusicGen이 멜로디 조건화를 제공하나 단일 축이며 취향 개념이 없다.
- AIVA가 MIDI 편집으로 축 분리에 가장 근접하나, 사용자의 취향을 학습하지 않고 프리셋을 선택하게 한다.
- **데이터 경계를 설계 원칙으로 명시한 서비스가 없다.** 모두 서버 처리 전제다.
- (실측 필요) 각 서비스의 최신 기능·정책은 조사 시점 기준이며, 구현 착수 전 재확인이 필요하다.

### □ SWOT 분석

| | 도움 | 방해 |
|---|---|---|
| **내부** | **Strength**<br>· 실측 완료된 1004곡 개발 코퍼스 보유<br>· 인제스트 파이프라인 구현·검증 완료<br>· 계층 구조를 계약으로 강제하는 아키텍처 확립<br>· 결정 기록(DECISIONS.md) 체계 운영 중<br>· GPU 보유 (GTX 1660Ti 6GB) | **Weakness**<br>· 1인 개발, 전 영역 단독 수행<br>· **VRAM 명목 6GB / 가용 4.8GB — 대형 생성 모델 학습 불가**<br>· 오디오 심층학습 실무 경험 부족<br>· 병행 과제(Fire-Lane)로 12월까지 가용 시간 제한<br>· 노트북 2대 환경 불일치 |
| **외부** | **Opportunity**<br>· 개인화 생성 영역에 선행 사례 부재<br>· 사전학습 오디오 모델(MERT·CLAP·Demucs) 공개<br>· 엣지 AI·데이터 주권 논의 확산<br>· 생성 AI 포트폴리오 수요 증가 | **Threat**<br>· 대형 서비스가 유사 기능을 먼저 출시할 가능성<br>· 생성 음악 저작권 법제 미확립<br>· 실존 가수 음색 관련 규제 강화 가능성<br>· 모델 라이선스 변경 리스크 |

### □ SWOT 교차 분석 기반 실행 전략

**[SO — 강점으로 기회를 잡는다]**
- 보유 코퍼스로 4축 분석 파이프라인을 먼저 완성하고, 취향 프로파일 시각화를 **생성 이전에 독립 산출물로 확보**한다. 생성이 미완이어도 포트폴리오가 성립한다.
- 사전학습 모델을 학습이 아니라 **특징 추출기로 사용**하여 VRAM 제약을 우회한다.

**[ST — 강점으로 위협에 대응한다]**
- 결정 기록 체계로 **"왜 이 설계인가"를 문서로 남긴다.** 기능이 아니라 판단 과정이 포트폴리오의 실질이며, 이는 대형 서비스가 유사 기능을 내도 무효화되지 않는다.
- 음색 게이트를 선제 구현하여 규제 강화 시에도 설계 변경이 불필요하게 한다.

**[WO — 약점을 보완해 기회를 취한다]**
- 6GB VRAM에서 **파인튜닝은 LoRA·어댑터로 한정**하고 전체 학습은 시도하지 않는다. 필요 시 클라우드 GPU를 단발성으로 임차한다.
- 병행 과제 기간에는 문서·설계·분석 단계를 진행하고, GPU 집약 단계는 12월 이후로 배치한다.

**[WT — 약점과 위협을 동시에 회피한다]**
- 생성 단계 미완 가능성을 인정하고 **P3(취향 프로파일)까지를 최소 완성 목표**로 고정한다. P4 이후는 확장 목표로 분리한다.
- 노트북 2대 환경을 Ubuntu 22.04 LTS로 통일하여 환경 불일치로 인한 시간 손실을 제거한다.

### □ STP 전략

**[Segmentation]**

| 세그먼트 | 특성 | 규모 |
|---|---|---|
| A. 음악 소비자 | 듣기만 함. 창작 경험 없음 | 최대 |
| B. 창작 지망 비전공자 | 만들고 싶으나 도구·이론 장벽 | 중간 |
| C. 아마추어 창작자 | DAW 사용 경험. 소재 탐색 필요 | 소수 |
| D. 전문 작곡가 | 완성도 요구 높음. AI 산출 신뢰 낮음 | 최소 |

**[Targeting]**

**1차 타깃: B (창작 지망 비전공자).** 취향은 뚜렷하나 그것을 음악으로 옮길 수단이 없는 집단이다. 시드곡 선택이라는 입력 방식이 이 집단의 장벽을 정확히 제거한다.

**2차 타깃: C (아마추어 창작자).** MIDI·스템 산출물이 이들에게 실질 가치를 갖는다.

D는 제외한다. 현행 생성 모델 품질로 전문가 요구를 충족할 수 없으며, 억지로 겨냥하면 모든 지표에서 미달한다.

**[Positioning]**

> "프롬프트를 쓰지 않는 음악 생성. 당신이 들어온 음악이 입력이다."

| 축 | 경쟁 서비스 | HATHOR |
|---|---|---|
| 입력 부담 | 프롬프트 작문 필요 | **아는 곡 고르기** |
| 개인화 | 세션 한정 | **누적 학습** |
| 데이터 | 서버 전송 | **기기 내 처리** |

### □ 추진 일정

**전제**: 2026-12-11까지 병행 과제(Fire-Lane)가 우선순위를 가진다. 해당 기간은 GPU 비집약·문서 중심 단계를 배치한다.

| 단계 | 내용 | 기간 | 상태 |
|---|---|---|---|
| **P0** | 저장소 골격, 계층 계약, CI, 도커 구성 | 2026-08 | **완료** |
| **P1** | 인제스트 — 스캐너·아티스트 파서·지문 정규화·영속화 | 2026-08 ~ 09 | **진행 (유닛 1/4 완료)** |
| **P2** | 4축 특징 추출 — 스템 분리, 음악·편곡·가사·가창 특징 | 2026-09 ~ 10 | 예정 |
| **P3** | 취향 잠재 표현 — 임베딩, 쌍대비교, 취향 벡터 | 2026-10 ~ 11 | 예정 |
| **P4** | 로컬 에이전트 — Tauri 앱, 백그라운드 분석, 동기화 | 2026-11 ~ 12 | 예정 |
| **—** | **Fire-Lane 파이널 프로젝트 마감 (12-11)** | | |
| **P5** | 심볼릭 생성 — 구조·화성·멜로디 | 2026-12 ~ 2027-01 | 예정 |
| **P6** | 편곡·렌더링 — 오디오 생성, 믹스 | 2027-01 | 예정 |
| **P7** | 가창 합성 및 안전 게이트 — 화자 무의존 설계, SV 임계 캘리브레이션 | 2027-01 ~ 02 | 예정 |
| **P8** | 통합·평가·배포 — 평가 리포트, 데모 모드 | 2027-02 | 예정 |

**최소 완성 목표(MVP)**: P4까지. 라이브러리 분석 + 취향 프로파일 + 로컬 에이전트가 동작하면 포트폴리오로 성립한다.
**확장 목표**: P5 이후. 미완 시 "설계 완료, 미구현"으로 명시하며 이는 감점 요인이 아니다.

### □ 차별성

| 구분 | 기존 접근 | HATHOR |
|---|---|---|
| **취향 입력** | 프롬프트에 형용사를 쓴다 | 라이브러리를 분석해 벡터로 만든다 |
| **개인화 지속성** | 세션 종료 시 소멸 | 취향 벡터가 누적·갱신된다 |
| **축 제어** | 곡 전체를 한 번에 생성 | 4축에 다른 시드 지정 가능 |
| **데이터 위치** | 서버 처리 | 원본 오디오가 기기를 떠나지 않는다 |
| **음성 사례** | 문제로 인식되지 않음 | 문제를 명시하고 조건부 생성으로 재정의 |
| **안전** | 정책 문구 | 동작하는 SV 임계 게이트 |
| **평가** | 주관 청취·FAD | 취향 벡터 거리로 정량화 |

### □ 주요 리스크 및 대응

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R-1 | **VRAM 6GB로 생성 모델 구동 불가** | 치명 | 학습 대신 사전학습 모델 추론 사용. 파인튜닝은 LoRA 한정. 필요 시 클라우드 GPU 단발 임차. **(해소 2026-08-11, D-0018 — Demucs 549MB 제약 없음, MERT는 10초 청크 분할 시 557MB. 배치 5시간 이내. **실측 정정 2026-08-16 — 1004곡 전량 5.5~7시간, 곡당 20~26초. 산출물 348MB, 누락 0**)** |
| R-2 | **병행 과제로 12월까지 가용 시간 부족** | 높음 | GPU 비집약 단계를 앞에 배치. MVP 범위를 P4로 고정 |
| R-3 | 스템 분리 품질 상한 | 중간 | 코퍼스가 전량 손실 압축(mp3)이므로 분리 품질에 원리적 한계가 있다. 지표 해석 시 명시 |
| R-4 | 한국어 강제정렬 정확도 | 중간 | SYLT 전량 부재로 정렬이 필수. 실패 시 가창축을 음역·음색 중심으로 축소 |
| R-5 | 생성 결과가 실존 가수와 유사 | 높음 | SV 임베딩 코사인 임계 게이트. 임계값은 P7 실측 캘리브레이션 |
| R-6 | 취향 벡터의 일반화 실패 | 높음 | 곡 ID가 아니라 잠재 공간에서 학습. 라이브러리 외 곡으로 홀드아웃 검증 |
| R-7 | 데스크톱 앱 배포 복잡도 | 중간 | Tauri 단일 플랫폼(Windows) 우선. 데모 모드로 설치 없는 체험 경로 확보 |
| R-8 | 노트북 2대 환경 불일치 | 중간 | Ubuntu 22.04 LTS + Python 3.12 핀 고정. 시스템 바이너리 버전 기록 |
| R-9 | 생성 음악 저작권 미확립 | 낮음 (포트폴리오 한정) | 사업화 전제하지 않음. 산출물 권리 관계를 문서에 명시 |
| R-10 | 범위 확대(scope creep) | 높음 | 한 번에 한 작업 단위, WIP 2 제한. 신규 착상은 결정 기록으로만 남기고 즉시 구현하지 않음 |

### □ 진행 중 미해결

리스크 표와 달리 이 절은 **작업 중 실측으로 드러난 확정 사안**을 담는다.

**닫힌 항목을 지우지 않고 아래 「닫힌 미해결」로 옮긴다** (D-0080). 예전 규약은
"해소되면 결정 기록으로 옮기고 여기서 지운다"였는데, 지우면 **"이건 왜 안 하기로
했지"를 다시 묻게 된다** — 결정 기록 80건에서 그 항목을 찾는 것보다 여기 한 줄이
싸다. ID 중복·정렬·닫힘 표시는 `tools/sync_decision_index.py`가 검사한다.

<!-- open-issues:begin -->

| # | 항목 | 상태 |
|---|---|---|
| O-3 | **제목 부가 표기 파서의 프로덕션 편입 여부** | 조사 스크립트는 괄호 깊이 카운팅으로 처리했고(D-0019) 실측 효과는 +0.6%p였다. 유닛 #2 수준의 정식 파서로 승격할지, 조회 전처리로 남길지 미결. D-0020 관찰: MB 레코딩 제목에도 `(Prod. …)`가 그대로 있어 MB 역시 정규화하지 않는다. |
| O-4 | **RESOLVED 판정에 오답이 섞인다** | `비 → Click-B`, `키네틱 플로우 → Letter flow`가 격차 10점 룰을 통과했다. 아티스트 84.3%는 성공률이 아니라 **상한선**이다. D-0019에서 이 오답이 곡 조회로 전파되는 사례(`효린 → Hyolyn` 4곡 전량 실패)도 확인됐다. |
| O-6 | **정규명 실패 시 태그 표기 재조회** | D-0019 실패 유형 (2)의 대응책. 조회 횟수가 두 배가 되고 회복 이득은 수십 건 수준이라 P2 착수 전에는 하지 않기로 했다. 비용 대비 효과 재평가 필요. |
| O-10 | **취향 모델은 보조 기능** | D-0029로 격하됐다. 주기능은 시드곡 퓨전(D-0011)이며 취향 모델은 "무엇을 만들지 모를 때"의 진입점이다. 수집 도구와 응답 30건(무작위 고정 평가 집합)은 보존하되 **추가 수집은 하지 않는다.** 재개 조건: 시드 퓨전 경로가 서고 취향 지표(M3) 설계가 가능해질 때. 학습 집합에는 층화·적응적 선택이 필요하다(무작위 30문항에서 특정 아티스트 10% 편중 실측). |
| O-11 | **캐싱·큐 인프라 미도입** | Redis·RabbitMQ 등을 붙이지 않기로 **결정했다**(미검토가 아니다). 현재 데이터는 1004건이고 전체 코사인이 CPU 2.3초다. 사용자 1명, 요청 0건/초에서 캐시는 성능이 아니라 운영 복잡도만 늘리며, WSL 실가용 RAM 4.8GB를 GPU 예산에서 깎는다. **도입 조건: 동시 사용자가 생겨 GPU 추출이 큐잉될 때. 그 시점에는 큐(RabbitMQ)가 먼저고 캐시(Redis)가 그다음이다.** 임베딩은 곡→벡터 결정론적 함수이므로 콘텐츠 해시 캐시의 히트율이 매우 높으나, 서버 측 임베딩 DB는 C-8(상용 음원 배포 불가)에 막힌다. |
| O-13 | **화면 설계서 부재** | 기획서 3부작 Part III에 화면 설계 절이 없다. 현재 인터페이스가 CLI뿐이므로 **지금 쓰면 추측이 된다**(GR-0.5). REQ-UI 착수 시점에 작성하며, 그때 사용자 조사가 수반되면 페르소나·사용자 여정지도도 함께 쓴다. **지금 쓰지 않는 이유는 형식이 불필요해서가 아니라 채울 근거가 없어서다** (D-0039). |
| O-14 | **문서 체계가 세 번 바뀌었다 — 다음엔 체계가 아니라 문서량을 본다** | D-0008(MASTER.md 단일화) → D-0015(강등) → D-0039(3축 선언) → D-0042(색인 자동화·항법). **체계를 자주 바꾸는 것 자체가 신호다.** 현재 `DESIGN.md` 1600줄이며 폐기된 MASTER.md(4302줄) 사유의 3분의 1을 넘었다. Part I(제안서)은 8월 10일 이후 거의 안 바뀌고 Part III(설계)는 매 세션 바뀌어 **갱신 주기가 다른 것이 한 파일에 있다.** 지금 자르지 않는다 — 1430줄은 아직 감당되고, 분할 자체가 네 번째 체계 변경이 된다. **분할 조건: Part III가 800줄을 넘거나 부록 A 외에 손유지 중복이 또 발견될 때.** 그때 `docs/plan/` 하위로 Part별 분리한다. 포트폴리오 제출용 기획서 docx가 필요하면 **새 문서를 쓰지 않고 `DESIGN.md` Part I~III에서 빌드한다** (D-0043) — 별도 파일은 즉시 두 번째 진실 공급원이 된다. |
| O-15 | **M0 게이트의 전제가 축 의존적이다 — 가사축 게이트가 정의되지 않았다** | D-0044 실측. 게이트의 전제는 "M0가 무너지면 아래 지표는 무의미하다"(D-0023)인데 **반복 감쇠 조건이 이를 반증했다** — M0 0.3850으로 반토막인데 M1 2.2배 · M2 2.5배로 다른 조건(M0 0.90대)과 같다. 오디오는 음색이 곡 특정성과 유사성을 겸해 게이트가 작동했으나 **가사에서는 다른 신호다.** M0는 반복 문구를, M1/M2는 어휘·주제를 잰다. **게이트 동작은 이번에 바꾸지 않았다** — 무엇으로 대체할지 근거가 없고 미달 시점에 게이트를 손대는 것은 형태가 합리화와 같다. R@10 전환은 논리가 성립하나 통과시켜도 M1이 2.6배(오디오 20.4배의 8분의 1)라 실익이 없어 D-0044에서 기각했다. **재검토 조건: 신경망 인코더 실측 후.** 그때 M0와 M1/M2의 상관을 다시 본다. 그때까지 가사축은 `--force`로 평가하며 그 사실을 인용에 함께 적는다. |
| O-16 | **문자 n-gram이 언어별 문자 밀도에 편향됐을 가능성** | D-0045 실측. 코퍼스 문자 비율은 한글 55% · 라틴 45%인데 **곡 기준으로는 한글 76% · 라틴 24%**다. 한글은 음절 하나가 1자, 영어는 자모 단위라 같은 뜻에 문자가 2~3배 들기 때문이다(곡당 평균 라틴 주 1,164자 대 한글 주 455자, 2.6배). **해싱 공간에서 영어 구간이 훨씬 많은 n-gram을 뿌리므로 같은 곡 안에서도 영어 후렴이 한국어 절보다 벡터를 더 차지했을 수 있다 (실측 필요).** 검증하려면 곡을 주 언어로 나눠 M0·M1·M2를 따로 낸다 — 하네스 변경 없이 라벨만 있으면 된다. **BGE-M3 실측이 먼저다** — 인코더가 이 문제를 함께 지우면 조사할 이유가 사라진다. |
| O-17 | **해시 · BGE-M3 앙상블 미시도** | D-0048. 두 표현의 강점이 상보적이다 — 해시는 M0 0.9174로 곡 지문에, BGE-M3는 M1 4.2배로 의미 유사성에 강하다. 하네스가 `--keys a+b` 다중 키와 `block_l2`를 이미 지원하므로 이론적으로 유망하다. **하지 않기로 했다** — 타임박스가 막으려던 상황이고(개선 3종 → 분할 정의 → 인코더 → 풀링 → 앙상블), 두 npz 저장소 병합이라 "조건 하나 더"가 아니며, 넘어도 오디오 M2 14.3배의 3분의 1이다. **재개 조건: 생성 경로가 종단간 관통한 뒤.** |
| O-19 | **구조 추출의 절대 임계가 결과를 지배한다** | D-0051 실측. 임계 0.3→0.7에서 평균 반복 비율이 0.4614→0.1869로 **2.5배** 달라지고 반복 없는 곡이 7.2%→41.0%로 는다. **어떤 값을 골라도 그 선택이 결론에 섞인다.** 원인 가설: 한국어 가사는 조사·어미가 겹쳐 서로 다른 절끼리도 기저 유사도가 높고 **그 기저값이 곡마다 다르다.** 절대값이 의미를 갖지 않는 공간에서 절대 임계를 쓰고 있으며, D-0031 중심화·D-0046 이방성과 같은 형태의 문제다. **다음: 곡 내부 유사도 분포 기반 상대 기준**(상위 비율 또는 중앙값 대비 배수). 절대 임계 0.5를 베이스라인으로 함께 낸다(D-0034). 그리고 **어느 쪽이 옳은지 판정할 라벨이 여전히 없다** — 구간에 절·후렴 라벨이 없고, 검증하려면 소수 곡에 손으로 붙여야 한다. |
| O-20 | **`--force` 재추출이 인덱스 중복을 남긴다** | D-0053. `lyrics extract --force`가 재추출하면서 인덱스에 덧붙여 2007줄이 됐다. 평가가 중복을 잡고 `ingest compact`가 정리했으므로 **시스템은 제 역할을 했다.** 다만 `--force`를 쓰는 사람이 원하는 것은 "다시 뽑기"이지 "덧붙이기"가 아니며, **두 명령이 항상 짝으로 다녀야 한다는 것이 계약에 드러나지 않는다.** 후보: `--force`가 인덱스를 먼저 비운다 / 종료 시 자동 정리한다 / 현행 유지하고 문서에 명시한다. **자동 정리는 위험하다** — 정리가 npz 없는 기록을 버리므로, 추출이 중간에 죽으면 자동 정리가 멀쩡한 이전 산출물까지 지울 수 있다. 실측 없이 고르지 않는다. |
| O-22 | **조성 추정 정확도를 잴 라벨이 없다 — 진단이 원인을 짚었다 (D-0056)** | D-0055 진단으로 **나란한조 비율 24.5%(낮음)**가 나와 원리적 한계가 아님이 확인됐고, 원인은 **선형 FFT가 저역에서 반음을 못 가르는 것**이었다(65Hz에서 한 빈 2.65반음). 반음 격자로 바꿔 합성 60음 전수 정확해졌다. **그래도 라벨은 여전히 없다.** 판정 수단은 분포 검사뿐이며, 재실측에서 D 편중이 줄고 **나란한조 비율이 오르는 것이 맞는 방향**이다 — 다른 원인이 사라지면 남는 애매함은 원리적 한계여야 한다. 격차 지표는 판별력 +0.0002로 폐기 대상이었으나 반음 격자에서 되살아날 여지가 있어 재측정 후 판단한다. |
| O-24 | **애매한 곡의 87%가 원인 미상이다** | D-0058. 분모를 고쳐 다시 재니 **애매한 15곡 중 나란한조 혼동은 2곡(13.3%)뿐**이다. 나머지 13곡은 K-S의 원리적 한계가 아니라 고칠 여지가 있는 문제다. 무엇인지는 아직 모른다 — O-23의 후보(프로파일 · 배음)와 같은 뿌리일 수 있으나 확인되지 않았다. **애매한 곡의 조성 교차표를 따로 내면** 특정 조에 몰리는지 볼 수 있고, 몰린다면 프로파일 쪽이다 (실측 필요). |
| O-25 | **베이스라인이 조건을 따라가는지 매번 확인해야 한다 — 구조적 방어를 한 곳에서 구현했다 (D-0062)** | D-0060. 일곱 번 겪었다 — 베이스라인 누락(D-0034) → 축 간 불일치(D-0053) → 분모 오류(D-0057) → `0.0`이 거짓(D-0058) → 프로파일 미추종(D-0059) → 배음 미추종(D-0060, 같은 함수에서 재발) → **비교 없이 이상 판정(D-0061).** **규칙 세 줄이다.** (1) 새 지표를 만들 때 **무엇과 비교할지** 정한다. (2) **그 비교선이 이길 수 있는지**도 본다 (D-0062) — 조건화가 질 수 없는 지표는 비교가 아니다. (3) **그 상한을 무엇으로 읽을지**도 정한다 (D-0063) — 0.0187 nats는 K-K 프로파일의 0.0616과 나란히 놓기 전엔 뜻이 없었다. 구조적 방어는 `harmony_prior.PriorCondition`에 구현했다: 조건 객체 하나를 모든 비교선이 받으므로 인자 추가를 잊을 수 없다. **D-0064에서 열 번째가 나왔고 형태가 달랐다 — 비교가 아니라 손잡이가 안 걸렸다.** `chroma(harmonic=...)`가 `cq_chroma`로 넘어가지 않아 조용히 무시됐다. **새 인자를 넣으면 그 인자가 실제로 결과를 바꾸는지 단위 검사로 고정한다.** **D-0065에서 열한 번째가 나왔고 또 달랐다 — 판정 규칙 자체가 대조 없이 쓰였다.** "귀무 λ*는 0이어야 한다"고 적었는데, 곡끼리 독립인 합성 자료에서도 0.1이 나온다. **대조를 먼저 돌렸으면 처음부터 안 적었을 규칙이다.** (4) **판정 규칙을 적을 때 그 규칙이 귀무 자료에서 어떻게 나오는지 먼저 돌려 본다.** `random_baseline`은 아직 인자를 낱개로 받는다 — 조성 추정이 종료돼(D-0061) 손댈 이유가 없어 두었고, 되살릴 때 같은 형태로 옮긴다. **다섯째 줄 (D-0086): 귀무 자료가 실측에서 확인된 구조를 재현하는지 본다.** 구조가 빠진 합성 자료는 규칙을 통과시킨다 — D-0083·D-0084·D-0086에서 **세 번 다 그렇게 통과했다.** 매번 (4)를 지켰는데 매번 통과한 이유가 그것이다. 최소한 코퍼스 평균 프로파일은 넣고 돌린다. |
| O-26 | **화음 가중을 근음으로 할지 3화음으로 할지 근거가 없다** | D-0063. `degree_weights`가 각 도수의 **근음** 크로마만 본다. 3화음(근음·3음·5음) 평균이 화음 존재를 더 잘 반영하지만, I와 vi가 3음 중 2음을 공유해 **판별력이 사라진다.** 판별력을 근거로 근음을 골랐고 **음악적으로 어느 쪽이 옳은지는 재지 않았다.** 라벨이 없어 재기 어렵다 (O-22). 귀로 판정하려 들면 손잡이 돌리기가 되므로 착수 전에 무엇과 비교할지부터 정한다 (O-25). |
| O-28 | **실측 수치가 코드와 문서에 흩어져 박히고 조용히 낡는다** | D-0074. D-0063이 "K-K 대비 14%"를 결정 기록·`harmony_generator` 독스트링·DESIGN 표에 각각 박았다. O-27이 닫히며 전제가 바뀌자 **세 곳이 동시에 틀렸고, 찾은 것은 `grep`이었다.** 결정 기록은 추가 전용(GR-0.2)이라 뒤에 갱신을 덧붙였고 코드 쪽은 고쳤다. **자동 검사 방법이 없다** — 숫자에 출처 표기를 강제하는 린트는 오탐이 크고, 실측값을 한 곳에 모으면 문서가 읽기 어려워진다. 후보: (a) 실측 수치를 결정 기록에만 두고 코드는 참조만 (b) 수치마다 D 번호 표기를 규약으로 (c) 그대로 두고 `grep`으로 관리. **무엇과 비교할지 정하기 전에는 착수하지 않는다** (O-25). |
| O-30 | **`DECISIONS.md` 분할 조건이 없다** | D-0080. O-14가 DESIGN에 대해 "Part III 800줄"을 정해 두었는데 **결정 기록에는 조건이 없다.** 지금 80건 4900줄로 **폐기된 MASTER.md(4302줄)를 이미 넘었다.** 기록당 평균이 62.6줄인데 **최근 20건은 88.3줄**로 길어지고 있다. 그리고 항법이 `grep`뿐이다 — 색인이 제목만 주므로 "이건 왜 이렇게 됐지"를 주제로 찾을 수 없다. **지금 쪼개지 않는다** — 문서 체계가 이미 네 번 바뀌었고(O-14) 분할이 다섯 번째가 된다. **분할 조건: 6000줄 또는 100건.** 그때 번호대별 파일로 나누고 색인 도구가 전부 긁게 한다. 주제 색인은 **손유지가 되는 순간 두 번째 진실 공급원이 되므로**(D-0042가 겪은 것) 무엇으로 자동 생성할지 정하기 전에는 착수하지 않는다 (O-25). |
| O-31 | **다이어토닉 제한이 버리는 몫 — 다시 열렸다 (D-0086)** | D-0085가 닫았다가 **판정 근거가 무너져 다시 연다.** 살아남은 것: 두 사전 거리의 **43.6%**가 버려지는 칸에서 나온다(귀무선과 무관한 항등식) · 두 출처가 같은 방향 · **버려지는 6칸 중 하나는 온음계 음**(장조 반음 11 = 이끔음, V화음 구성음)이며 감화음을 코드 풀에서 뺐다는 이유로 음 자체가 버려지고 있었다. 무너진 것: "다이어토닉보다 **더** 곡 고유하다" — `실측 - within`의 **0점이 0이 아니라 +0.055**이고 실측 +0.037은 그보다 아래다. **등가선(`matched`)을 넣었고 다시 돌리면 닫힌다.** **D-0087이 도구를 고쳤다** — 난수기 하나를 순서대로 쓰고 있어 선을 더하면 다른 선의 수가 밀렸고 귀무선을 한 번만 뽑았다. **D-0085의 표는 재현되지 않는다.** 두 출처를 새 코드로 다시 돌려야 닫힌다. |
| O-32 | **순서 조건화 — 어휘는 천장을 쳤다** | D-0082. `paired` 극한 0.1199가 어휘 조건화의 상한이며 마디 수를 늘려도 그 이상 안 간다. 사전을 더 뾰족하게 만드는 축은 O-27에서 소진했다(`other`가 최선, 넣을수록 나빠진다). **남은 것은 배열이다** — D-0062가 범위를 줄이며 "이 판정이 통과해도 화성 어휘 조건화이지 진행 조건화가 아니다"라고 미리 적어 둔 자리다. 후보: (a) 크로마 시계열에서 마디별 코드 추정 (b) 코퍼스 진행 통계. **지표가 없다** — 도수 히스토그램은 순서에 불변이므로 편집 거리 계열이 필요하고, **무엇과 비교할지 정하기 전에는 착수하지 않는다** (O-25). **O-33 뒤로 간다** (D-0085) — 어휘가 곡 정보를 44% 버리는 채로 순서를 다듬는 것은 순서가 틀렸다. |
| O-33 | **비음계 질량의 정체 — "곡 고유"가 "음악적"은 아니다** | D-0085. 버려지는 칸이 곡마다 다르다는 것은 측정됐으나 **왜 다른지는 안 나왔다.** 후보 넷(실제 차용화음·조성 추정 오차·크로마 누설·스템 분리 잔재)이 **전부 곡마다 다르다.** 조성 추정이 반음 틀리면 온음계 질량이 통째로 비음계 칸으로 넘어가고 그것도 곡 고유로 잡힌다. **`keys.jsonl`의 `margin`과 비음계 질량의 상관을 보면 갈린다** — 값싸고 새 자료가 필요 없다. **그 전에는 어휘 확장에 착수하지 않는다.** 착수 전 판정 규칙을 귀무 자료에서 먼저 돌린다 (O-25 (4)). |
| O-34 | **난수 흐름 결함이 `evaluate_harmony_output`에도 있다** | D-0087. `run`이 난수기 하나로 쌍 추첨·도수 치환·무작위 사전을 차례로 뽑는다. **비교선을 하나 더하면 뒤 선의 추첨이 통째로 밀린다** — `evaluate_degree_restriction`에서 실제로 그 일이 났다(D-0086 → D-0087). **D-0079~D-0082의 수치도 같은 상태다.** 지금 고치면 D-0082의 실측이 재현 불가가 되므로 **다음에 그 도구를 손댈 때 함께 고친다.** 고치는 법은 정해져 있다 — 선마다 `[seed, 선 번호, 반복]`으로 흐름을 가르고 귀무선을 여러 번 뽑아 쌍별로 평균한다. |

<!-- open-issues:end -->

### □ 닫힌 미해결

**판단의 근거는 결정 기록에 있다.** 여기 남기는 것은 "이미 답이 나온 질문"이라는
사실과 그 번호뿐이다. O-23은 중간 상태(D-0058)와 최종 판정(D-0061)이 두 행으로
남아 있었고 **최종만 남긴다** — 중간 상태의 근거는 결정 기록에 그대로 있다.

<!-- closed-issues:begin -->

| # | 항목 | 결말 |
|---|---|---|
| O-12 | **가사축 M0 정의 부적합 — 판정 완료 (D-0044)** | **해결.** 홀짝 분할이 가사 구간 구조와 정렬돼 있었음이 확인됐다(무작위화로 5.8~11.5σ 상승). **그러나 갭의 절반도 메우지 못한다** — 최선 0.9174, 게이트 0.95. 정의 문제는 실재했으나 **표현이 한계**다. 비대칭 70/30은 실측 기각(0.8839 ±0.0106). **다음은 신경망 문장 인코더이며 언어 구성 실측이 선행한다.** 분할 축이 있으므로 개선의 인코더 몫을 떼어낼 수 있다. |
| O-18 | **가사 구간 분할에서 1곡이 조용히 빠진다 — 해결 (D-0050)** | **해결.** 원인은 후보 셋 어느 것도 아니었다. **폴백 판정이 정리 *전에* 이뤄지는 순서 문제였다** — 빈 줄이 하나뿐이라 블록 2개로 폴백을 건너뛴 뒤, 정리가 8자 미만 블록을 버려 1개가 됐고 되돌아갈 길이 없었다. 정리를 먼저 하고 폴백을 판정하도록 고쳤다(16구간으로 복구). **기존 1003곡은 불변이며 회귀 테스트로 고정했다.** |
| O-21 | **화성 어휘 조건화 — 닫힘 (D-0082)** | D-0054에서 열었다. D-0063이 "작지만 실재한다"로 닫았고 D-0074가 스템 사전에서 이득이 세 배가 되며 다시 열었다. **효과 크기가 측정됐다** — 같은 시드 1000개에서 두 참조곡의 도수 히스토그램 거리가 `mix` 0.1310 · `other` 0.1817이고 쌍 단위 승률 75%다. **순서는 여전히 시드가 정한다** — 그것은 O-32다. |
| O-23 | **검은건반 33.5% — 기각. 이상이 아니었다 (D-0061)** | **해결(기각).** 후보 넷(조율·로그압축·프로파일·배음)을 전부 기각한 끝에, **33.5%가 애초에 이상한 값이 아님을 확인했다.** 검은건반 조는 12개 중 5개라 **균등이 41.7%**이고 코퍼스는 그보다 8.2%p 낮다 — 흰건반 쪽으로 이미 편향돼 있다. "명백히 높다"는 판정이 도메인 상식이었지 베이스라인이 아니었다. **D-0034 계열 일곱 번째이며, 앞의 여섯이 "비교 대상이 조건을 안 따라감"인 데 비해 이번은 "비교를 아예 안 함"이다.** 세션 넷을 썼다. |
| O-27 | 크로마 대비가 교과서 프로파일의 30%뿐이다 → **닫힘 (D-0074)** | D-0063에서 열었다. **원인은 타악이었다.** 드럼을 뺀 `other` 스템에서 달성 가능 폭이 0.0187 → **0.0534**로 K-K 장조(0.0616) 대비 30.4% → **86.7%**가 됐다. (b) 창별 중앙값은 +5.9%로 기각(D-0065), (c) 대비 정규화도 기각. **넣을수록 나빠진다** — `other` > `+bass` > `+vocals`. 저역은 CQ 격자에서 뭉개지고(D-0056) 보컬은 비브라토로 번져 둘 다 균등 성분을 도로 넣는다. **폭 확대가 잡음이 아닌 근거는 `self` 승률 93.5%다** — 잡음이면 반쪽 간에 재현될 이유가 없다. |
| O-29 | **출력 차이 — 닫힘 (D-0082)** | D-0078에서 열었다. **전달된다.** 사전 등록 규칙 둘 다 통과했고, "지표는 세 배인데 출력은 그대로"는 **단위 착각이었다** — 교차 엔트로피 폭은 균등에서 벗어난 정도의 제곱에, 전변동 거리는 그 자체에 비례하므로 CE 3.14배는 애초에 TV 1.77배다(실측 극한 1.51배). 지난 세션의 "2마디"도 설명된다 — 마디 불일치 평균이 `mix` 1.24 · `other` 1.79이고 **8마디 한 표본이 둘 다 2를 낸 것이다.** |

<!-- closed-issues:end -->

### □ 참고 자료

**모델·기법**
- Défossez et al., *Hybrid Transformers for Music Source Separation* (Demucs v4)
- Li et al., *MERT: Acoustic Music Understanding Model with Large-Scale Self-supervised Training*
- Wu et al., *Large-scale Contrastive Language-Audio Pretraining* (CLAP)
- Copet et al., *Simple and Controllable Music Generation* (MusicGen)
- Bradley & Terry, *Rank Analysis of Incomplete Block Designs* (쌍대비교 모델)
- Radford et al., *Robust Speech Recognition via Large-Scale Weak Supervision* (Whisper)

**데이터셋**
- MTG-Jamendo Dataset (CC 라이선스 음원)
- Free Music Archive (FMA)
- MusicBrainz / AcoustID (지문 기반 메타데이터)

**도구**
- Chromaprint / fpcalc, mutagen, Montreal Forced Aligner
- Tauri, FastAPI, PostgreSQL + pgvector

**내부 문서**
- `docs/DECISIONS.md` — 설계 결정 기록 (전체 색인은 부록 A)
- `tools/probe_id3.py`, `tools/probe_artist.py` — 코퍼스 실측 스크립트

(실측 필요) 참고 문헌의 서지 정보는 구현 착수 시 정확한 출처로 보강한다.

---

# Part II. 요구사항 분석서

## □ REQ-HATHOR 요구사항 체계도

```text
HATHOR
├─ REQ-ING  인제스트          라이브러리 스캔 · 정규화 · 영속화
├─ REQ-ANL  4축 분석          스템 분리 · 음악/편곡/가사/가창 특징
├─ REQ-TST  취향 모델         잠재 표현 · 쌍대비교 · 취향 벡터
├─ REQ-AGT  로컬 에이전트     데스크톱 앱 · 백그라운드 작업 · 동기화
├─ REQ-GEN  생성             구조 · 화성 · 멜로디 · 편곡 · 렌더링
├─ REQ-LYR  작사             주제 · 운율 · 음절 정렬
├─ REQ-VOC  가창             화자 무의존 합성 · 표현 제어
├─ REQ-QUA  품질 · 안전       화자 게이트 · 유사도 검사 · 적합도
├─ REQ-API  서빙             동기화 API · 생성 작업 API
├─ REQ-UI   인터페이스        시드 선택 · 프로파일 시각화 · 결과 관리
├─ REQ-INF  인프라           저장소 · 큐 · 배포 · CI
└─ REQ-EVL  평가 · 검증       지표 · 절제 실험 · 재현성
```

## □ 요구사항 ID 체계 정의

`REQ-{그룹}-{번호}` 형식. 번호는 그룹 내 3자리 일련번호.

| 항목 | 규칙 |
|---|---|
| 그룹 | 위 체계도의 3문자 코드 |
| 번호 | 001부터 순차. 폐기해도 재사용하지 않는다 |
| 우선순위 | **M**(Must, MVP 필수) / **S**(Should) / **C**(Could) |
| 단계 | 해당 요구사항이 구현되는 P단계 |
| 상태 | 미착수 / 진행 / 완료 / 폐기 |

**MVP 경계**: 우선순위 M은 P4까지의 범위에 한정한다. P5 이후 요구사항은 최대 S를 부여한다. 이는 미완 시 MVP 판정에 영향을 주지 않기 위한 장치다.

## □ 세부 기능 요구사항 정의서

### 1. REQ-ING (인제스트)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-ING-001 | 라이브러리 루트를 재귀 순회하여 오디오 파일을 발견한다 | M | P1 | 완료 | 순회 순서는 결정적이어야 한다 |
| REQ-ING-002 | ID3 태그에서 제목·아티스트·앨범·가사를 원문 그대로 추출한다 | M | P1 | 완료 | 실측상 유효 프레임은 이 4종뿐 |
| REQ-ING-003 | 오디오 헤더에서 샘플레이트·채널·길이·비트레이트를 읽는다 | M | P1 | 완료 | 디코딩하지 않는다 |
| REQ-ING-004 | 단일 파일 실패가 전체 순회를 중단시키지 않는다 | M | P1 | 완료 | 실패는 분류하여 별도 기록 |
| REQ-ING-005 | 이전 스냅샷과 대비하여 신규·변경·소실·무변화를 판정한다 | M | P1 | 완료 | mtime + size 기준 |
| REQ-ING-006 | 아티스트 문자열을 분해하여 주 표기·별칭·협업자 후보를 산출한다 | M | P1 | 진행 | 정규화하지 않고 후보와 신뢰도만 제시 |
| REQ-ING-007 | Chromaprint 지문을 산출하여 곡 신원을 정규화한다 | M | P1 | 미착수 | ISRC 전량 부재로 주 경로 |
| REQ-ING-008 | AcoustID·MusicBrainz 조회로 MBID·발매일·정규 아티스트명을 복구한다 | S | P1 | 미착수 | 조회 성공률은 게이트가 아님 |
| REQ-ING-009 | 스캔 결과를 PostgreSQL에 업서트한다 | M | P1 | 미착수 | 지문 기반 정규 식별자 사용 |
| REQ-ING-010 | 가사가 없는 곡에 대해 STT 폴백 경로를 제공한다 | C | P2 | 미착수 | 실측상 USLT 100%이므로 우선순위 낮음 |
| REQ-ING-011 | 가사 언어를 태그가 아니라 본문에서 판별한다 | M | P2 | 미착수 | 실측상 한국어 가사가 eng로 기재됨 |

### 2. REQ-ANL (4축 분석)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-ANL-001 | 스템 4종(보컬·드럼·베이스·기타)으로 분리한다 | M | P2 | 미착수 | 편곡축·가창축의 전제 |
| REQ-ANL-002 | 스템 분리를 GPU에서 수행하고 CPU 폴백을 제공한다 | M | P2 | 미착수 | (실측 필요 — 6GB VRAM 동작 확인) |
| REQ-ANL-003 | 분리 결과를 캐시하여 재분석을 회피한다 | M | P2 | 미착수 | 예상 용량 약 35GB |
| REQ-ANL-004 | 음악축: 조성·BPM·박자·코드 진행·멜로디 윤곽을 산출한다 | M | P2 | 미착수 | |
| REQ-ANL-005 | 편곡축: 악기 구성·음색 특성·섹션 구조·다이나믹을 산출한다 | M | P2 | 미착수 | 스템 의존 |
| REQ-ANL-006 | 가사축: 주제·어휘·정서가·운율 패턴을 산출한다 | M | P2 | **착수 (베이스라인)** | 문자 n-gram 해싱, D-0036. 주제·정서는 미착수 |
| REQ-ANL-007 | 가창축: 음역·비브라토·발성 밝기·표현 강도를 산출한다 | M | P2 | 미착수 | 보컬 스템 의존 |
| REQ-ANL-008 | 한국어 강제정렬로 가사-오디오 시간 정보를 생성한다 | M | P2 | 미착수 | SYLT 전량 부재로 필수 |
| REQ-ANL-009 | 사전학습 모델(MERT·CLAP) 임베딩을 산출한다 | M | P2 | 미착수 | 학습이 아니라 추론 |
| REQ-ANL-010 | 모델 입력 직전에만 리샘플하고 원본 샘플레이트를 유지한다 | M | P2 | 미착수 | 모델별 요구 SR 상이 |
| REQ-ANL-011 | 분석 실패 곡을 분류하여 기록하고 전체를 중단시키지 않는다 | M | P2 | 미착수 | |

### 3. REQ-TST (취향 모델)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-TST-001 | 4축 특징을 축별 잠재 공간으로 투영한다 | M | P3 | 미착수 | |
| REQ-TST-002 | 라이브러리 전체로부터 초기 취향 벡터를 산출한다 | M | P3 | 미착수 | 전량 양성 사례 전제 |
| REQ-TST-003 | 적응적 쌍대비교로 상대 순위 신호를 수집한다 | M | P3 | 미착수 | 최소 20회, 이후 건너뛰기 허용 |
| REQ-TST-004 | 쌍 선택은 임베딩 거리와 모델 불확실성을 함께 고려한다 | S | P3 | 미착수 | 무작위 추출은 정보량 낮음 |
| REQ-TST-005 | 쌍대비교 결과를 잠재 공간 위에서 적합한다 | M | P3 | 미착수 | 곡 ID 학습 시 일반화 실패 |
| REQ-TST-006 | 취향 벡터와 가중치를 영속화하고 갱신 이력을 남긴다 | M | P3 | 미착수 | |
| REQ-TST-007 | 생성 결과 피드백으로 취향 벡터를 갱신한다 | S | P5 | 미착수 | |
| REQ-TST-008 | 축별 취향 분해를 조회 가능한 형태로 제공한다 | M | P3 | 미착수 | |
| REQ-TST-009 | 라이브러리 외 곡으로 취향 모델을 홀드아웃 검증한다 | S | P3 | 미착수 | 일반화 검증 |

### 4. REQ-AGT (로컬 에이전트)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-AGT-001 | **원본 오디오와 음원 파일을 서버로 전송하지 않는다** | M | P4 | 미착수 | **불변 원칙. 위반 시 설계 결함** |
| REQ-AGT-002 | 데스크톱 앱으로 배포한다 | M | P4 | 미착수 | Tauri. Windows 우선 |
| REQ-AGT-003 | 분석을 백그라운드에서 수행하고 중단·재개를 지원한다 | M | P4 | 미착수 | |
| REQ-AGT-004 | 곡 단위 진행률을 표시한다 | M | P4 | 미착수 | |
| REQ-AGT-005 | 부분 분석 결과로도 취향 벡터를 산출한다 | M | P4 | 미착수 | 전량 완료를 기다리지 않는다 |
| REQ-AGT-006 | 취향 벡터와 특징 임베딩만 서버와 동기화한다 | M | P4 | 미착수 | 전송 대상을 화이트리스트로 강제 |
| REQ-AGT-007 | 오프라인에서 분석과 프로파일 조회가 동작한다 | S | P4 | 미착수 | 생성만 서버 필요 |
| REQ-AGT-008 | GPU 미보유 환경에서 CPU 경로로 동작한다 | M | P4 | 미착수 | 소요 시간 안내 필수 |
| REQ-AGT-009 | 라이브러리 4축 프로파일을 로컬에서 시각화한다 | M | P4 | 미착수 | 생성 없이도 성립하는 산출물 |

### 5. REQ-GEN (생성)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-GEN-001 | 시드곡을 축별로 독립 지정할 수 있다 | S | P5 | 미착수 | 본 시스템의 핵심 차별점 |
| REQ-GEN-002 | 곡 구조(섹션 배열)를 생성한다 | S | P5 | 미착수 | |
| REQ-GEN-003 | 코드 진행을 생성한다 | S | P5 | 미착수 | |
| REQ-GEN-004 | 멜로디를 생성한다 | S | P5 | 미착수 | |
| REQ-GEN-005 | 심볼릭 단계 산출물을 MIDI로 내보낸다 | S | P5 | 미착수 | |
| REQ-GEN-006 | 편곡·악기 배치를 생성한다 | S | P6 | 미착수 | |
| REQ-GEN-007 | 오디오로 렌더링하고 믹스한다 | S | P6 | 미착수 | |
| REQ-GEN-008 | 단계별 중간 산출물을 열람하고 부분 재생성할 수 있다 | S | P6 | 미착수 | |
| REQ-GEN-009 | 동일 시드에서 동일 산출물을 생성한다 | M | P5 | 미착수 | 재현성 CI 대상 |
| REQ-GEN-010 | 시드 위에 미세조정 파라미터를 적용할 수 있다 | C | P6 | 미착수 | |

### 6. REQ-LYR (작사)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-LYR-001 | 취향 벡터의 가사축을 조건으로 가사를 생성한다 | S | P6 | 미착수 | |
| REQ-LYR-002 | 멜로디 음절 수에 맞춰 가사를 정렬한다 | S | P6 | 미착수 | 한국어 음절-음표 대응 |
| REQ-LYR-003 | 기존 가사를 그대로 복제하지 않는다 | M | P6 | 미착수 | n-gram 중복 검사 |

### 7. REQ-VOC (가창)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-VOC-001 | 화자 무의존 설계로 가창을 합성한다 | S | P7 | 미착수 | 특정 화자에 종속되지 않음 |
| REQ-VOC-002 | 공개 다화자 DB를 베이스로 하고 취향 가중치를 델타로 적용한다 | S | P7 | 미착수 | |
| REQ-VOC-003 | 단일 화자 기여도에 상한을 둔다 | M | P7 | 미착수 | 음색 복제 방지 |
| REQ-VOC-004 | 개발자 본인 보컬 시드 녹음은 P7까지 연기한다 | C | P7 | 미착수 | 선행 설계가 화자 무의존이므로 필수 아님 |

### 8. REQ-QUA (품질 · 안전)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-QUA-001 | **SV 임베딩 코사인 유사도가 임계를 넘으면 산출을 차단한다** | M | P7 | 미착수 | 실존 가수 식별 가능성 차단 |
| REQ-QUA-002 | 임계값을 실측으로 캘리브레이션한다 | M | P7 | 미착수 | 임의 설정 금지 |
| REQ-QUA-003 | 생성 멜로디와 기존 곡의 유사 구간을 탐지한다 | S | P6 | 미착수 | |
| REQ-QUA-004 | 생성 결과와 취향 벡터의 거리를 적합도로 산출한다 | S | P6 | 미착수 | 정량 평가의 기반 |
| REQ-QUA-005 | 차단 시 사유와 재생성 가이드를 제시한다 | S | P7 | 미착수 | 차단만 하고 끝내지 않는다 |

### 9. REQ-API (서빙)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-API-001 | 취향 벡터 동기화 엔드포인트를 제공한다 | M | P4 | 미착수 | |
| REQ-API-002 | 전송 페이로드에 오디오 데이터가 포함되지 않음을 서버에서 검증한다 | M | P4 | 미착수 | 스키마 수준 강제 |
| REQ-API-003 | 생성 작업을 큐에 등록하고 상태를 조회한다 | S | P5 | 미착수 | 비동기 |
| REQ-API-004 | 생성 결과와 스템·MIDI를 다운로드한다 | S | P6 | 미착수 | |
| REQ-API-005 | API 스키마를 OpenAPI로 문서화한다 | M | P4 | 미착수 | |

### 10. REQ-UI (인터페이스)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-UI-001 | 라이브러리 4축 프로파일을 시각화한다 | M | P4 | 미착수 | 2차원 투영 + 축별 분포 |
| REQ-UI-002 | 쌍대비교 온보딩 화면을 제공한다 | M | P3 | 미착수 | |
| REQ-UI-003 | 시드곡을 축별로 선택하는 화면을 제공한다 | S | P5 | 미착수 | |
| REQ-UI-004 | 생성 진행 상태와 단계별 산출물을 표시한다 | S | P5 | 미착수 | |
| REQ-UI-005 | 생성 곡 라이브러리와 버전 이력을 제공한다 | S | P6 | 미착수 | |
| REQ-UI-006 | 설치 없이 체험 가능한 데모 모드를 제공한다 | M | P8 | 미착수 | 사전 분석 프로파일 사용 |

### 11. REQ-INF (인프라)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-INF-001 | 계층 구조를 정적 계약으로 강제한다 | M | P0 | 완료 | import-linter 4종 |
| REQ-INF-002 | CI에서 린트·타입·계약·테스트를 검사한다 | M | P0 | 완료 | |
| REQ-INF-003 | 시크릿 스캔을 CI에 포함한다 | M | P0 | 완료 | |
| REQ-INF-004 | 결정적 생성 경로를 CI에서 검증한다 | M | P0 | 완료 | |
| REQ-INF-005 | 개발 환경을 컨테이너로 재현 가능하게 구성한다 | M | P0 | 완료 | Compose 5종 |
| REQ-INF-006 | 벡터 검색을 위해 pgvector를 사용한다 | M | P3 | 부분 | 확장 설치 완료, 미사용 |
| REQ-INF-007 | 생성 작업을 메시지 큐로 비동기 처리한다 | S | P5 | 부분 | RabbitMQ 기동, 미사용 |
| REQ-INF-008 | 산출물을 오브젝트 스토리지에 보관한다 | S | P6 | 부분 | MinIO 기동, 미사용 |
| REQ-INF-009 | 개발 기기 간 런타임 버전을 일치시킨다 | M | P1 | 진행 | Python 3.12 핀 완료, OS 통일 예정 |

### 12. REQ-EVL (평가 · 검증)

| ID | 요구사항 | 우선 | 단계 | 상태 | 비고 |
|---|---|---|---|---|---|
| REQ-EVL-000 | 임베딩 검색 품질을 M0/M1/M2로 측정한다 | M | P1 | **완료 (9조건 실측)** | 태그 동치류 라벨. D-0023 설계 · D-0024 결과 |
| REQ-EVL-001 | 파이프라인 산출 이력(manifest)을 기록한다 | M | P2 | 미착수 | 입력·버전·시드·산출물 해시 |
| REQ-EVL-002 | 취향 모델을 홀드아웃으로 평가한다 | S | P3 | 미착수 | |
| REQ-EVL-003 | 축별 절제 실험으로 각 축의 기여를 측정한다 | S | P6 | 미착수 | 축 분리 설계의 실증 |
| REQ-EVL-004 | 프롬프트 기반 베이스라인과 취향 적합도를 비교한다 | S | P6 | 미착수 | |
| REQ-EVL-005 | 동일 시드 재실행 시 산출물 동일성을 검증한다 | M | P5 | 미착수 | |
| REQ-EVL-006 | 평가 리포트를 자동 생성한다 | C | P8 | 미착수 | |

## □ 비기능 요구사항 (NFR)

### 1. 정확성 및 안전성 (Accuracy & Safety)

| ID | 요구사항 | 기준 |
|---|---|---|
| NFR-ACC-001 | 인제스트 정규화 성공률 | 코퍼스 대비 99% 이상. **실측 100% (1004/1004)** |
| NFR-ACC-002 | 외부 조회 보강률 | 게이트가 아님. 측정만 수행 (실측 필요) |
| NFR-ACC-003 | 스템 분리 품질 | (실측 필요) SDR 기준. 손실 압축 코퍼스로 인한 상한 명시 |
| NFR-ACC-004 | 강제정렬 정확도 | (실측 필요) 수동 검수 표본 대비 |
| NFR-SAF-001 | 화자 유사도 차단 | SV 코사인 임계 초과 시 100% 차단. 미탐 0을 목표로 임계를 보수측으로 편향 |
| NFR-SAF-002 | 가사 복제 방지 | 기존 가사와 연속 n-gram 일치 시 차단 |

**오류 방향 설계**: 화자 게이트에서 미탐(실존 가수와 유사한데 통과)과 오탐(무관한데 차단)의 비용은 비교 대상이 아니다. 미탐은 법적 분쟁을 낳고 오탐은 재생성만 유발한다.
임계를 대칭으로 두지 않고 미탐 회피 방향으로 편향시킨다.

### 2. 성능 및 가용성 (Performance & Availability)

| ID | 요구사항 | 기준 |
|---|---|---|
| NFR-PER-001 | 인제스트 스캔 속도 | **실측: 1004곡 10.8초 (GTX 1660Ti 환경, drvfs 경유)** |
| NFR-PER-002 | 스템 분리 처리량 | (실측 필요) GPU/CPU 각각 곡당 소요 시간 |
| NFR-PER-003 | 취향 벡터 산출 | 부분 결과로도 산출 가능. 전량 완료 대기 금지 |
| NFR-PER-004 | 생성 응답 | 비동기 작업. 진행률 제공 |
| NFR-AVL-001 | 로컬 분석 가용성 | 서버 없이 동작 |
| NFR-AVL-002 | 분석 중단 복원 | 프로세스 종료 후 재시작 시 진행 지점부터 재개 |

### 3. 보안 및 법규 준수 (Security & Compliance)

| ID | 요구사항 | 기준 |
|---|---|---|
| NFR-SEC-001 | **오디오 비이동 원칙** | 서버 수신 페이로드에 오디오 바이트가 존재해서는 안 된다. 스키마 검증으로 강제 |
| NFR-SEC-002 | 시크릿 관리 | 자격증명을 저장소에 커밋하지 않는다. CI 스캔으로 강제 |
| NFR-SEC-003 | 개발 코퍼스 비배포 | 상용 음원 1004곡을 저장소·배포물에 포함하지 않는다 |
| NFR-SEC-004 | 테스트 픽스처 | 실제 음원 대신 합성 오디오를 생성하여 사용한다 |
| NFR-SEC-005 | 의존성 취약점 | CI에서 정기 검사 |

### 4. 운영성 및 재현성 (Maintainability & Reproducibility)

| ID | 요구사항 | 기준 |
|---|---|---|
| NFR-MNT-001 | 계층 경계 | import-linter 계약 4종 전부 KEPT |
| NFR-MNT-002 | 타입 검사 | mypy --strict 통과 |
| NFR-MNT-003 | 테스트 커버리지 | 75% 이상. **현재 실측 85%** |
| NFR-MNT-004 | 결정 기록 | 설계 판단은 DECISIONS.md에 배경·후보·선택·근거·결과·한계 형식으로 기록 |
| NFR-REP-001 | 산출물 동일성 | 동일 입력·시드에서 기기가 달라도 산출물이 동일해야 한다 |
| NFR-REP-002 | 절대경로 배제 | 산출물에 기기별 절대경로를 포함하지 않는다 |
| NFR-REP-003 | 런타임 고정 | Python 버전을 핀으로 고정한다 |

### 5. 호환성 (Compatibility)

| ID | 요구사항 | 기준 |
|---|---|---|
| NFR-CMP-001 | 오디오 포맷 | mp3 우선. 확장 가능한 구조 유지 (실측상 코퍼스 100% mp3) |
| NFR-CMP-002 | 샘플레이트 | 44.1kHz / 48kHz 혼재 허용. 통일하지 않는다 |
| NFR-CMP-003 | 파일명 인코딩 | 유니코드 정규화 적용. NFD 입력 방어 |
| NFR-CMP-004 | 데스크톱 플랫폼 | Windows 우선. macOS·Linux는 확장 |
| NFR-CMP-005 | GPU 미보유 환경 | CPU 경로 정식 지원 |

## □ 제약사항 및 전제

| # | 구분 | 내용 |
|---|---|---|
| C-1 | 자원 | GPU는 GTX 1660Ti 단일. 명목 6GB 중 디스플레이 점유를 뺀 가용 4.8GB (D-0018). 대형 모델 전체 학습 불가 |
| C-2 | 자원 | 개발 기기 2대. 리전(GPU 보유, RAM 8GB) / 광인사(GPU 없음, RAM 16GB) |
| C-3 | 시간 | 2026-12-11까지 병행 과제 우선. HATHOR는 후순위 |
| C-4 | 인력 | 1인. 코드 리뷰어 부재. 정적 검사와 테스트로 대체 |
| C-5 | 데이터 | 개발 코퍼스 전량 손실 압축(mp3). 스템 분리 품질에 원리적 상한 |
| C-6 | 데이터 | 발매일·ISRC·동기 가사 전량 부재. 지문 조회와 강제정렬로 대체 |
| C-7 | 데이터 | 코퍼스 전량 양성 사례. 음성 사례 부재 |
| C-8 | 법적 | 개발 코퍼스는 상용 음원. 배포·서비스 코퍼스로 사용 불가 |
| C-9 | 법적 | 실존 가수 음색이 식별 가능한 산출물 금지 |
| C-10 | 법적 | 생성 음악의 저작권 귀속 미확립. 사업화 전제하지 않음 |
| C-11 | 환경 | drvfs 경유 볼륨에서 파일시스템 감시 불가 (실측 확인) |

## □ 요구사항 변경 이력

| 일자 | 변경 | 사유 |
|---|---|---|
| 2026-08-10 | 초판 작성 | MASTER.md를 대체하는 기획서 3부작 체계로 전환 |
| 2026-08-10 | REQ-AGT 그룹 신설 | 아키텍처를 서버 처리에서 로컬 에이전트 방식으로 변경 |
| 2026-08-10 | REQ-ING-002 필드 축소 | ID3 전수 조사 결과 유효 프레임 4종 확인 |
| 2026-08-10 | REQ-ING-006 범위 축소 | 아티스트 파서가 정규화하지 않고 후보만 제시하도록 변경 |

---

# Part III. 상세 설계서

## 1. 시스템 아키텍처

### □ 전체 파이프라인

```text
┌─────────────────── 사용자 기기 (로컬 에이전트) ───────────────────┐
│                                                                   │
│  음원 라이브러리 (원본 위치 참조, 복제하지 않음)                    │
│         │                                                         │
│         ▼                                                         │
│  [1] 인제스트                                                      │
│      스캔 → 태그 추출 → 아티스트 파싱 → 지문 → 업서트              │
│         │                                                         │
│         ▼                                                         │
│  [2] 스템 분리 (Demucs)                                            │
│      보컬 / 드럼 / 베이스 / 기타                                    │
│         │                                                         │
│         ├──────────┬──────────┬──────────┐                       │
│         ▼          ▼          ▼          ▼                       │
│  [3] 음악축    편곡축      가사축     가창축                        │
│      조성·BPM  악기·음색   주제·정서  음역·표현                     │
│      코드진행  섹션구조    운율      비브라토                       │
│         │          │          │          │                       │
│         └──────────┴────┬─────┴──────────┘                       │
│                         ▼                                         │
│  [4] 취향 벡터 산출                                                │
│      축별 잠재 투영 + 쌍대비교 가중치                               │
│                         │                                         │
└─────────────────────────┼─────────────────────────────────────────┘
                          │  전송: 취향 벡터 + 임베딩만
                          │  ★ 오디오 바이트는 이 선을 넘지 않는다
                          ▼
┌─────────────────────── 서버 ──────────────────────────────────────┐
│                                                                   │
│  [5] 생성 파이프라인                                               │
│      구조 → 화성 → 멜로디 → 편곡 → 가사 → 가창 → 믹스              │
│                         │                                         │
│                         ▼                                         │
│  [6] 품질·안전 게이트                                              │
│      화자 동일성 / 멜로디 유사도 / 취향 적합도                      │
│                         │                                         │
│                         ▼                                         │
│  [7] 산출물 반환                                                   │
│      오디오 + 스템 + MIDI                                          │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### □ 계층 구조 (GR-2)

정적 계약(import-linter)으로 강제되는 6계층. **계약 정의는 `core/pyproject.toml`이 진실이며
여기에는 근거만 둔다** — 설정을 문서에 다시 적으면 반드시 어긋난다 (D-0042 · D-0043).

| 계층 | 책임 | 의존 방향 |
|---|---|---|
| `domain` | 엔티티·값객체·포트. 외부를 전혀 모른다 | 없음 |
| `application` | 유스케이스·오케스트레이션. 포트에만 의존 | → domain |
| `engines` | 무거운 AI 연산 단위. 엔진 간 직접 호출 금지 | → domain |
| `infrastructure` | 포트 구현체. DB·파일시스템·외부 API·모델 | → domain, application |
| `interfaces` | CLI·REST·워커 진입점. 비즈니스 로직 없음 | → application |
| `shared` | 설정·텔레메트리 | 없음 |

**계약 4종**
1. 도메인은 외부를 모른다 — `domain`이 다른 계층을 import하지 않는다
2. 애플리케이션은 구현체를 모른다 — `application`이 `infrastructure`를 import하지 않는다
3. 엔진 간 직접 호출 금지 — 엔진끼리 서로 import하지 않는다. 조합은 `application`이 한다
4. 계층 순서 — 위 의존 방향을 역행하지 않는다

**엔진 vs 배관의 구분 기준**: 엔진은 무거운 AI 연산이다. 인제스트는 파일을 읽고 표를 만드는 배관이므로 엔진이 아니며 `application` +
`infrastructure`에 배치한다.

### □ 트리거 타이밍 — 명시적 실행, 상시 감시 없음

파일시스템 실시간 감시는 채택하지 않는다. drvfs 경유 외장 볼륨에서 inotify watch 등록은 성공하나 이벤트가 전달되지 않음을 실측으로 확인했다. 등록이 성공하므로
상위 라이브러리는 오류 없이 무동작 상태가 되며, 감시가 조용히 실패한다.

| 시점 | 동작 |
|---|---|
| 사용자 명시적 실행 | 라이브러리 재스캔 → 델타 판정 |
| 델타 발생 곡 | 스템 분리 및 4축 분석 수행 |
| 무변화 곡 | 분석 건너뜀. 기존 결과 재사용 |
| 취향 벡터 | 분석 완료분으로 즉시 재산출 |
| 생성 요청 | 사용자 트리거. 큐에 등록 |

**델타 감지의 값이 나오는 지점**: 인제스트 자체는 1004곡 10.8초라 절감 여지가 없다. 실질 이득은 곡당 GPU 연산이 발생하는 스템 분리에서 나온다. 따라서 델타 기반
건너뛰기는 P2에서 구현한다.

### □ 모듈 경계 — 로컬 ↔ 서버

| 항목 | 로컬 | 서버 |
|---|---|---|
| 원본 오디오 | 보유 | **보유하지 않음** |
| 스템 | 보유 | 보유하지 않음 |
| 특징 임베딩 | 산출 | 수신 |
| 취향 벡터 | 산출 | 수신·저장 |
| 생성 모델 | 없음 | 보유 |
| 생성 산출물 | 수신 | 산출 |

**경계 강제 방법**: 서버 수신 스키마에 오디오 바이트 필드를 정의하지 않는다. 필드가 없으면 전송 자체가 불가능하다. 정책이 아니라 타입으로 강제한다.

## 2. 데이터 설계

### □ 데이터 규칙 (GR-3)

**저장소 역할 분담** — 어느 저장소가 진실인지 흐려지면 같은 사실이 두 곳에서 갈린다.

| 저장소 | 역할 | 금지 |
|---|---|---|
| **PostgreSQL** | 관계 데이터의 **단일 진실 공급원**. 벡터(pgvector) 포함 | — |
| MongoDB | 원본 태그·트레이스·로그 등 비정형 | **진실 공급원으로 사용 금지** |
| Redis | 캐시·락·진행률 | **영속 데이터 저장 금지.** 유실을 전제로 설계 (미도입, O-11) |
| 오브젝트 스토리지 | 오디오·모델·산출물 | 메타데이터 저장 금지 |

**공통 컬럼** — 모든 PostgreSQL 테이블에 포함한다.

| 컬럼 | 타입 | 규칙 |
|---|---|---|
| `{entity}_id` | UUID | PK. `gen_random_uuid()`. **순차 정수 금지** (열거 공격 방지) |
| `created_at` | TIMESTAMPTZ | 생성 시 기록. **변경 금지** |
| `updated_at` | TIMESTAMPTZ | 트리거로 자동 갱신 |
| `deleted_at` | TIMESTAMPTZ | 사용자 데이터는 소프트 삭제 |

**시각은 전부 TIMESTAMPTZ(UTC)로 저장한다.** 표시 시점에만 로컬 변환한다.

**모델링**

1. 기본 설계는 **3NF**에서 시작한다.
2. 반정규화는 **측정된 성능 문제가 있을 때만** 하고, 사유와 일관성 보장 방법을 남긴다.
3. 모델 구조는 API 응답이 아니라 **저장 기준**으로 설계한다.
4. **ORM 엔티티를 API 응답으로 직접 반환하지 않는다.** 반드시 DTO를 거친다.
5. JSONB는 (1) 스키마가 유동적이고 (2) 검색 조건이 아닌 것에만 쓴다. 검색 조건이 되면 컬럼으로 승격한다.

**ML 데이터**

| 규칙 | 내용 |
|---|---|
| 피처 버전 | 추출기 변경 시 버전 증가. **기존 버전 보존** |
| 임베딩 버전 | 모델 버전 필수 기록. 모델이 다르면 다른 레코드 |
| 취향 프로파일 | 갱신 시 새 버전 INSERT. **기존 버전 불변** |
| 외부 참조 | 라이선스 NOT NULL. 오디오 미보관 제약 (C-8) |
| 대용량 | Git에 바이너리 금지. `var/` 하위는 gitignore |

### □ 개발 코퍼스 실측

| 항목 | 값 |
|---|---|
| 곡 수 | 1,004 |
| 총 재생시간 | 64.06시간 |
| 총 용량 | 9,286,565,870 바이트 (8.65 GiB) |
| 포맷 | mp3 100% |
| 샘플레이트 | 44.1kHz 76.8% (771곡) / 48kHz 23.2% (233곡) |
| 읽기 실패 | 0 |
| 파일명 규칙 | `아티스트-제목.mp3` 100% 일관 |
| 파일명 정규화 | NFC 100% (NFD 0건) |

### □ ID3 프레임 실측 (1004곡 전수)

| 프레임 | 보유율 | 용도 |
|---|---|---|
| TIT2 (제목) | 100% | 사용 |
| TPE1 (아티스트) | 100% | 사용 |
| TALB (앨범) | 100% | 사용 |
| USLT (가사) | 100% | 사용 |
| APIC (앨범아트) | 100% | 존재 여부만 기록 |
| POPM (평점) | 100% | **미사용 — 전곡 동일 최고값, 분산 0** |
| TSSE (인코더) | 62.8% | 미사용 |
| TSOA/TSOP/TSOT (정렬키) | 3.7/0.8/0.7% | 미사용 |
| **TDRC·TDRL·TYER·TDAT (발매일)** | **0%** | 외부 조회로 복구 |
| **TSRC (ISRC)** | **0%** | 외부 조회로 복구 |
| **SYLT (동기 가사)** | **0%** | 강제정렬로 생성 |
| TRCK·TPE2·TCON·TPOS | 0% | 필드 미정의 |

**해석**: POPM 소유자 필드가 `Windows Media Player 9 Series`로 기재되어 있다. 라이브러리가 WMP로 관리되었으며, WMP가 평점은 기록하되
발매일은 채우지 않는 특성이 실측 결과를 설명한다. 태그 출처를 신뢰할 근거가 약하므로 지문 기반 정규화가 주 경로가 된다.

### □ 아티스트 표기 실측

| 항목 | 값 |
|---|---|
| 고유 아티스트 문자열 | 247 |
| 괄호 포함 | 37.5% (376곡) |
| 쉼표 포함 | 6.8% (68곡) |
| 앰퍼샌드 포함 | 0.5% (5곡) |
| 구분자 없음 | 58.9% (591곡) |
| `feat.` `with` `and` `/` `·` | **0건** |

**핵심**: 괄호 안 내용은 피처링이 아니라 **동일 아티스트의 별칭 병기**다. `아이유(IU)` 64곡, `BIGBANG (빅뱅)` 50곡, `태연 (TAEYEON)`
24곡. 괄호를 구분자로 처리하면 376곡에서 유령 아티스트가 생성된다.

**반례**: `Earth, Wind & Fire`(쉼표가 이름의 일부), `권순일(어반자카파)`(괄호가 소속), `잔나비 최정훈`(구분자 없는 다중), `GD&TOP`(유닛명
여부 판정 불가).

### □ 좌표 없는 정규화 — 식별자 체계

| 식별자 | 범위 | 안정성 | 부여 시점 |
|---|---|---|---|
| `source_key` | 라이브러리 루트 기준 상대경로 | 파일 이동 시 변경 | 스캔 |
| `fingerprint` | Chromaprint 지문 | 인코딩 동일 시 안정 | 지문 산출 |
| `track_id` | 내부 UUID | 영구 | 업서트 |
| `mbid` | MusicBrainz ID | 영구 | 외부 조회 성공 시 |

**2단 식별 전략**: `source_key`는 스캔 단위 멱등 키일 뿐 곡의 정규 신원이 아니다. 여러 `source_key`가 하나의 `track_id`로 병합될 수 있다.
절대경로를 쓰지 않는 이유는 기기마다 라이브러리 루트가 달라 산출물 동일성이 깨지기 때문이다.

### □ 테이블 명세 (주요)

**`tracks`** — 정규화된 곡

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `track_id` | UUID PK | |
| `fingerprint` | TEXT UNIQUE | Chromaprint |
| `mbid` | UUID NULL | 외부 조회 성공 시 |
| `title` | TEXT | |
| `duration_ms` | INT | |
| `release_date` | DATE NULL | 태그 부재. 조회로만 채움 |
| `isrc` | TEXT NULL | 태그 부재. 조회로만 채움 |
| `created_at` | TIMESTAMPTZ | |

**`track_sources`** — 곡과 파일의 대응 (1:N)

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `source_key` | TEXT PK | 루트 기준 상대경로 |
| `track_id` | UUID FK | |
| `file_size_bytes` | BIGINT | 델타 판정 |
| `modified_at` | TIMESTAMPTZ | 델타 판정 |
| `sample_rate_hz` | INT | |
| `channels` | SMALLINT | |
| `bitrate_bps` | INT | |

**`artists`** / **`track_artists`** — 아티스트와 참여

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `artist_id` | UUID PK | |
| `normalized_key` | TEXT UNIQUE | 괄호·공백 제거 + 소문자 |
| `primary_name` | TEXT | |
| `aliases` | TEXT[] | 괄호 별칭 |
| `mbid` | UUID NULL | |
| `parse_confidence` | ENUM | HIGH/MEDIUM/LOW |
| `ambiguity_reasons` | TEXT[] | LOW인 경우 사유 |

> **미해결(2026-08-11)**: `parse_confidence` · `ambiguity_reasons` · 원문 `raw`는
> 아티스트가 아니라 **파싱 사건**의 속성이라 `track_artists`에 있어야 한다.
> `아이유(IU)`는 HIGH, `울랄라세션, 아이유(IU)`는 MEDIUM인데 `normalized_key`가
> 같아 `artists` 한 행으로 수렴하므로 나중 스캔이 앞의 값을 덮는다.
> 유닛 #3의 MusicBrainz 조회 결과 저장 방식과 함께 결정한다.

**`track_features`** — 4축 특징

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `track_id` | UUID FK | |
| `axis` | ENUM | musical/arrangement/lyrical/vocal |
| `embedding` | vector | pgvector |
| `scalar_features` | JSONB | BPM·조성 등 해석 가능한 값 |
| `model_version` | TEXT | 재현성 |

**`taste_vectors`** — 사용자 취향

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `user_id` | UUID | |
| `axis` | ENUM | |
| `vector` | vector | pgvector |
| `source` | ENUM | library/pairwise/feedback |
| `version` | INT | 갱신 이력 |

**`pairwise_comparisons`** — 쌍대비교 응답

| 컬럼 | 타입 |
|---|---|
| `user_id` | UUID |
| `left_track_id` / `right_track_id` | UUID |
| `winner` | ENUM(left, right) |
| `axis` | ENUM |
| `responded_at` | TIMESTAMPTZ |

## 3. 인제스트 상세 설계

### □ 처리 순서

```text
파일 발견 (sorted 강제)
  → 헤더·태그 읽기 (디코딩 없음)
  → 실패 격리 (분류 후 계속)
  → 델타 판정 (mtime + size)
  → 아티스트 파싱 (후보 제시)
  → 지문 산출 (Chromaprint)
  → 외부 조회 (AcoustID → MusicBrainz)
  → 업서트
```

### □ 실패 분류

| 분류 | 조건 |
|---|---|
| `UNREADABLE_FILE` | OS 수준 읽기 실패 |
| `CORRUPT_HEADER` | 태그·헤더 파싱 실패 |
| `MISSING_AUDIO_STREAM` | 스트림 정보 부재 또는 길이 0 이하 |
| `TAG_DECODE_ERROR` | 태그 디코딩 실패 |
| `PERMISSION_DENIED` | 권한 없음 |
| `UNEXPECTED_ERROR` | 그 외 |

**격리 원칙**: 어떤 단일 파일도 전체 순회를 중단시키지 않는다. 프로세스 종료 코드는 실패율이 임계를 넘을 때만 비정상이 된다.

### □ 델타 이벤트

| 이벤트 | 조건 | 후속 처리 |
|---|---|---|
| `DISCOVERED` | 이전 스냅샷에 없음 | 전체 분석 |
| `MODIFIED` | mtime 또는 size 변경 | 전체 재분석 |
| `UNCHANGED` | 둘 다 동일 | 분석 건너뜀 |
| `REMOVED` | 이전에 있었으나 발견되지 않음 | 소실 처리 |

**소실 오판 방지**: 외장 볼륨 미연결 시 전량이 `REMOVED`로 판정된다. 소실 비율이 임계(50%)를 넘으면 경고하고 자동 삭제를 수행하지 않는다.

**`REMOVED`는 실패가 아니다.** 실패 통계와 분리하여 기록한다.

### □ 아티스트 파서

**출력**

| 필드 | 내용 |
|---|---|
| `raw` | 원문 보존 |
| `primary` | 주 후보 `ArtistCandidate(name, aliases)` |
| `collaborators` | 쉼표 분리 후보. 각 원소가 `ArtistCandidate` |
| `normalized_key` | 괄호·공백 제거 + 소문자. 후보 단위로도 산출 |
| `confidence` | HIGH / MEDIUM / LOW |
| `ambiguity_reasons` | 판정 불가 사유 |

**신뢰도 규칙**

| 등급 | 조건 |
|---|---|
| HIGH | 구분자 없음, 또는 괄호만 존재 |
| MEDIUM | 쉼표 분리이며 각 토큰이 2자 이상 |
| LOW | 짧은 토큰, `&`만 존재, 괄호 내용이 다수 곡에 반복 출현(소속 의심) |

**설계 원칙**: 파서는 정규화하지 않는다. 정규명 확정은 외부 조회의 책임이다. `아이유(IU)`는 한글이 앞이고 `BIGBANG (빅뱅)`은 영문이 앞이므로 주-부 순서
규칙을 문자열만으로 세울 수 없다.

## 4. 4축 특징 설계

| 축 | 특징 | 의존 | 산출 형태 |
|---|---|---|---|
| **음악** | 조성, 조성 안정성, BPM, 박자, 코드 진행, 멜로디 윤곽, 음정 도약 분포 | 원곡 | 스칼라 + MERT 임베딩 |
| **편곡** | 악기 구성, 스펙트럴 센트로이드·롤오프, 섹션 경계, 다이나믹 레인지, 스테레오 폭 | **스템** | 스칼라 + 임베딩 |
| **가사** | 주제 분포, 어휘 다양성, 정서가, 운율·각운, 화자 인칭 | USLT + 정렬 | 언어모델 임베딩 |
| **가창** | 음역대, 비브라토 속도·깊이, 발성 밝기, 어택 특성, 표현 강도 | **보컬 스템** | 스칼라 + 임베딩 |

**축별 진입 가능 시점**

- 가사축: 즉시 가능 (USLT 100% 확보)
- 음악축: 원곡만으로 가능
- 편곡축·가창축: **스템 분리 선행 필수.** P2 병목

**모델 입력 샘플레이트**

| 모델 | 요구 SR |
|---|---|
| Demucs (htdemucs) | 44.1kHz |
| MERT | 24kHz |
| CLAP | 48kHz |

원본을 유지하고 각 모델 입력 직전에만 리샘플한다. 사전 통일은 어느 모델에도 최적이 아니며 정보 손실만 발생시킨다.

## 5. 취향 모델 설계

### □ 문제 재정의

라이브러리는 전량 양성 사례다. 선호도 이진 분류를 학습할 음성 사례가 존재하지 않는다.

| 접근 | 필요 데이터 | 판정 |
|---|---|---|
| 선호도 예측 | 양성 + 음성 | 성립 불가 |
| **조건부 생성** | **양성만** | **채택** |

"이 곡을 좋아할 것인가"는 음성 사례를 요구하지만 "이런 느낌으로 생성하라"는 양성만으로 성립한다. 균일 양성 데이터가 결함이 아니라 전제가 된다.

### □ 취향 신호 3층

| 층 | 신호 | 성격 | 갱신 |
|---|---|---|---|
| 1. 라이브러리 | 보유 곡 전체 | 전역 사전확률 | 재스캔 시 |
| 2. 쌍대비교 | A vs B 선택 | 상대 순위 | 온보딩·재학습 |
| 3. 시드곡 | 생성 시 참조곡 | 지역 조건 | 생성마다 |

**층이 다르므로 충돌하지 않고 누적된다.** 시드가 지정하지 않은 부분을 사전확률이 메운다.

### □ 쌍대비교

**목적**: 절대 라벨 없이 상대 순위 신호를 만든다. A를 고르면 B는 그 맥락에서 상대 음성이 되며, 음성 사례 부재 문제를 외부 데이터 없이 완화한다.

**쌍 선택**: 임베딩 공간에서 거리가 먼 쌍을 우선하고, 이후 현재 모델의 예측 불확실성이 높은 쌍(확률 0.5 근방)을 선택한다. 무작위 추출은 유사곡 쌍에서 정보량이 0에
수렴한다.

**적합**: Bradley-Terry 계열 쌍대비교 모델. **반드시 잠재 공간 위에서 적합한다.** 곡 ID에 대한 선호만 학습하면 라이브러리 밖으로 일반화되지 않아 신규
생성곡에 대해 아무것도 말해주지 못한다.

**시행 횟수**: 최소 20회, 이후 건너뛰기 허용. 재학습 시 반복 부담을 방지한다.

**호칭**: 본 기법은 랜덤 포레스트가 아니다. 문서·발표에서 혼용하지 않는다.

### □ 축별 분해

취향 벡터는 4축 각각에 대해 독립적으로 산출한다. 사용자가 발라드 멜로디를 선호하면서 힙합 편곡을 선호하는 것은 모순이 아니며, 축을 합치면 이 정보가 소실된다.

## 6. 생성 파이프라인 설계

### □ 단계 분리

```json
[시드곡 + 취향 벡터]
       ↓
1. 구조     섹션 배열 (intro/verse/chorus/bridge/outro)
       ↓
2. 화성     코드 진행 (조성·기능화성)
       ↓
3. 멜로디   음정·리듬 (MIDI)
       ↓                    ← 여기까지 심볼릭. 검사·편집 가능
4. 편곡     악기 배치·보이싱
       ↓
5. 가사     주제 → 운율 → 음절 정렬
       ↓
6. 가창     보컬 합성
       ↓
7. 믹스     스템 합성·마스터링
       ↓
[품질·안전 게이트] → 통과 시 산출, 미통과 시 사유와 함께 차단
```

**심볼릭 우선 이유**: 전 구간을 오디오로 직접 생성하면 중간 산출물을 검사·편집할 수 없고, 어느 단계가 문제인지 진단할 수 없다. 심볼릭 단계를 분리하면 MIDI 내보내기가
부가 산출물로 얻어진다.

**축별 시드 적용 지점**

| 축 | 영향 단계 |
|---|---|
| 음악 | 1·2·3 (구조·화성·멜로디) |
| 편곡 | 4 (편곡) |
| 가사 | 5 (가사) |
| 가창 | 6 (가창) |

### □ 결정성

전 파이프라인은 시드 고정 시 결정적으로 동작한다. 시드는 사용자에게 노출되는 명시적 입력이며, 동일 시드 재실행 시 산출물 동일성을 CI에서 검증한다.

## 7. 가창 및 안전 게이트 설계

### □ 화자 무의존 설계

| 구성 | 내용 |
|---|---|
| 베이스 | 공개 다화자 데이터베이스 |
| 델타 | 취향 화자 가중치 |
| 상한 | 단일 화자 기여도 상한 |

특정 화자에 종속되지 않으므로 개발자 본인 보컬 시드 녹음은 필수가 아니며 P7까지 연기한다.

### □ 화자 동일성 게이트

```text
생성 보컬
   ↓
SV 임베딩 추출
   ↓
실존 가수 임베딩과 코사인 유사도 계산
   ↓
임계 초과? ── 예 → 차단 + 사유 제시 + 재생성 가이드
   │
   아니오
   ↓
통과
```

**임계값**: 임의로 설정하지 않는다. P7에서 실측 캘리브레이션한다. 미탐(유사한데 통과)과 오탐(무관한데 차단)의 비용이 비대칭이므로 임계를 미탐 회피 방향으로 편향시킨다.

**허용 범위**: 실존 가수 음색 자체를 금지하는 것이 아니라 **식별 가능한 산출물**을 금지한다. 공개 다화자 DB 베이스 + 취향 화자 가중치 델타 방식은 허용된다.

## 8. 로컬 에이전트 설계

### □ 구성

| 계층 | 기술 | 역할 |
|---|---|---|
| 셸 | Tauri (Rust) | 창·시스템 접근·배포 |
| UI | React + TypeScript | 웹 대시보드와 컴포넌트 공유 |
| 분석 사이드카 | Python | 인제스트·스템 분리·특징 추출 |
| 로컬 저장 | SQLite | 분석 결과·진행 상태 |

**Python 사이드카를 두는 이유**: 오디오 분석 생태계(mutagen·librosa·torch·demucs)가 Python에 있다. Rust로 재구현하는 비용이 이득을
넘는다. 이미 구현된 인제스트 코드를 그대로 재사용한다.

### □ 작업 관리

| 요구 | 처리 |
|---|---|
| 장시간 실행 | 백그라운드 작업 큐 |
| 중단·재개 | 곡 단위 체크포인트. SQLite에 진행 상태 기록 |
| 진행 표시 | 곡 단위 진행률 + 잔여 시간 추정 |
| 부분 결과 활용 | 완료분만으로 취향 벡터 산출 |
| GPU 부재 | CPU 폴백. 착수 전 예상 소요 시간 안내 |

### □ 전송 경계 강제

```python
# 서버 수신 스키마에 오디오 필드를 정의하지 않는다.
# 필드가 없으면 전송 자체가 타입 수준에서 불가능하다.

class TasteSyncPayload:
    user_id: UUID
    axis: Axis
    vector: list[float]
    model_version: str
    track_count: int
    # audio, waveform, stem, file_path 등 어떤 오디오 필드도 없다
```

정책 문구가 아니라 스키마로 강제한다. 리뷰나 규율에 의존하지 않는다.

## 9. 서빙 API 명세 (개요)

| 메서드 | 경로 | 설명 |
|---|---|---|
| `POST` | `/v1/taste/sync` | 취향 벡터 동기화 |
| `GET` | `/v1/taste/{user_id}` | 취향 벡터 조회 |
| `POST` | `/v1/pairwise/next` | 다음 비교 쌍 요청 |
| `POST` | `/v1/pairwise/respond` | 비교 응답 제출 |
| `POST` | `/v1/generate` | 생성 작업 등록 |
| `GET` | `/v1/generate/{job_id}` | 작업 상태 조회 |
| `GET` | `/v1/generate/{job_id}/result` | 산출물 조회 |
| `GET` | `/v1/demo/profile` | 데모 모드 사전 분석 프로파일 |

전체 스키마는 OpenAPI로 문서화한다.

## 10. 평가 설계

### □ 지표

| 대상 | 지표 | 기준 |
|---|---|---|
| 인제스트 | 정규화 성공률 | 99% 이상 (**실측 100%**) |
| 임베딩 | M0 자기일관성 top-1 | 0.95 이상. **게이트**. **실측 1.0000** (홀짝 분할, D-0040) |
| 임베딩 | M1 앨범 검색 P@10 | **실측 0.0699 (무작위 0.0034 대비 20.4배)** |
| 임베딩 | M2 아티스트 검색 P@10 | **실측 0.2503 (무작위 0.0175 대비 14.3배)** |
| 임베딩 | 전체 쌍 평균 코사인 | **실측 -0.0006**. 1에 가까우면 허브 곡이 생긴다 (D-0030) |
| 퓨전 | M4 시드 아티스트 균형 | **실측 coverage 0.2137 (무작위 0.0280 대비 7.6배, 상한 0.8867)**. **진단용이며 최적화 목표가 아니다** (D-0035) |
| 생성 | M5 출력 도수 거리 | 참조곡을 바꿨을 때 진행이 갈리는 정도 (O-29 · D-0082). 시드 1000개 · 쌍 100 · 8마디. **실측 `other` 0.1817 · `mix` 0.1310 · 쌍 단위 승률 75.0%**. 극한(사전 가중치 거리)은 0.1199 / 0.0795이며 **8마디 초과분 34%는 되튐이지 정보가 아니다.** 귀무선 `shuffled` 0.2679 대비 67.8% — 코퍼스가 화성 어휘를 공유한다 |
| 생성 | M6 비음계 몫 | 다이어토닉 제한이 버리는 대비의 비율 (O-31 · D-0086). **등가선(`matched`)과 견준다** — 치환 귀무선들은 0점이 0이 아니다. 실측 `other` 0.4357 · `mix` 0.4670이며 **거리의 44%가 버려지는 것은 항등식이다.** "더 곡 고유한가"는 등가선 대비로 재실측 필요 |

정본 뷰: `var/ingest/mert-layers`의 `layer00` + **중심화** (D-0027 · D-0031).
| 스템 분리 | SDR | (실측 필요). 손실 압축 상한 명시 |
| 강제정렬 | 정렬 정확도 | (실측 필요). 수동 검수 표본 대비 |
| 취향 모델 | 홀드아웃 순위 상관 | (실측 필요) |
| 생성 결과 | 취향 벡터 거리 | 프롬프트 베이스라인 대비 |
| 생성 결과 | FAD | 참고 지표. 절대 기준 아님 |
| 안전 게이트 | 미탐률 | 0 목표 |
| 재현성 | 동일 시드 산출물 일치 | 100% |

### □ 검색 평가 하네스 (D-0023)

취향 모델 이전에 **임베딩 자체가 쓸 만한지**부터 판정한다. 라벨은 외부 조회 없이
태그 동치류(TALB·TPE1, 각 100% 커버)로 만든다. 정규 신원이 아니라 동치류만
필요하므로 O-4(RESOLVED 오답)의 영향을 받지 않는다.

| 지표 | 정의 | 라벨 |
|---|---|---|
| M0 | 곡의 청크를 두 조각으로 나눠 한쪽으로 나머지를 top-1으로 찾는 비율 | 불필요 |

**M0의 분할 규칙은 설정이다** (D-0040). `odd-even`(기본, 오디오축 정본)과 `random`(무작위 균등 분할, 반복 평균±표준편차) 중 고른다. 오디오의
10초 청크는 곡 구조와 정렬될 이유가 없지만 **가사 구간은 빈 줄 경계, 즉 곡 구조 그 자체**라 홀짝이 절·후렴을 갈라놓을 수 있다. 기본값을 바꾸면
D-0031~D-0038의 M0 수치와 비교할 수 없으므로 `odd-even`을 유지하고 회귀 테스트로 고정한다.

M0는 top-1과 함께 **순위 진단**(MRR · R@5 · R@10 · 실패 순위 중앙값)과 **해석적 베이스라인**(무작위 top-1 = 1/n, 무작위 순위 중앙값 =
(n+1)/2)을 낸다 (D-0041). top-1만으로는 실패가 순위 2에 몰려 있는지 수백 등에 흩어져 있는지 구분할 수 없고, **그 둘은 처방이 정반대다.**
| M1 | top-k 중 같은 앨범 비율, MAP@k | TALB |
| M2 | top-k 중 같은 아티스트 비율, MAP@k (같은 앨범은 후보에서 제외) | TPE1 |

**M0 게이트의 전제는 축마다 다르다 (D-0044).** 게이트는 "M0가 무너지면 아래 지표는
무의미하다"를 전제한다. 오디오축에서는 음색·믹스가 **곡 특정성과 유사성을 겸해** 성립한다.
**가사축에서는 반증됐다** — 반복 감쇠 조건이 M0 0.3850(반토막)인데 M1 2.2배·M2 2.5배로
M0 0.90대 조건과 같았다. 가사에서 M0는 **반복 문구**를, M1/M2는 **어휘·주제**를 재며
둘이 다른 신호다. **가사축 게이트를 무엇으로 둘지는 미해결이다 (O-15).**

**미달의 성격은 순위로 가른다.** R@10이 게이트를 넘으면 정답이 상위권에 있다는 뜻이고
표현이 무너진 것이 아니다(가사 8192: top-1 0.9174, R@10 0.9715, 실패 순위 중앙값 3.8).
넘지 못하면 표현이 곡을 특정하지 못하는 것이다(반복 감쇠: R@10 0.4688, 실패 순위 831.1).
**두 상황은 처방이 정반대이며, D-0041 이전에는 같은 문구로 보고되고 있었다.**

- **베이스라인 2종 필수**: 무작위(시드 고정), MFCC 평균. 측정값 단독 인용 금지.
- MAP은 `min(R, k)`로 정규화한다. k로 나누면 지표가 앨범 크기 분포를 재게 된다.
- CPU 수 초. GPU 없는 기기에서 도는 것이 요건이다.
- **저장소를 둘 이상 합칠 때는 블록별 단위 정규화(`--block-l2`)가 필수다.** MERT 768차원과
  MFCC 40차원을 그냥 이어붙이면 코사인이 노름 큰 쪽에 압도당해 작은 블록이 무시된다.
  블록 정규화 후 concat하면 전체 코사인이 블록 코사인의 평균이 되어 기여가 균등해진다.
- **실측 결과는 D-0024.** 9조건 비교에서 **MFCC 40차원이 MERT 768차원을 이겼다**(M2 기준 47% 우위).
  원인 미확정이며 O-8로 등재. 스템은 MERT 임베딩에 기여하지 않음이 확인됐다.
- **공간 뭉침은 D-0030.** 전체 쌍 평균 코사인 **0.9476**으로 허브 곡이 존재한다.
  시드 퓨전이 중점 대신 코퍼스 중심으로 빨려가는 것이 실측으로 확인됐다.
  중심화 도입 후 **전 지표가 개선됐다**(D-0031): 평균 코사인 0.9476 → -0.0006,
  M1 MAP +14.1%, M2 MAP +13.0%, M0 유지. **기본값으로 승격**했으며 `--raw`로만 끈다.
  시드 퓨전에서 허브 곡이 사라지고 유사도 범위가 0.98대 → 0.42~0.33으로 정상화됐다.
- **레이어 곡선 종결은 D-0027.** 0~3이 단조 감소이고 6의 반등도 0을 넘지 못한다.
  **layer00이 최댓값이며 레이어 축은 소진했다.** 정본은 `var/ingest/mert-layers`의 `layer00`.
  재현성 검산 2회 통과(별도 배치·저장소에서 네 지표 완전 일치).
- **레이어 실측은 D-0026.** **MERT 마지막 레이어가 측정한 전 레이어 중 최악이었다.**
  `layer00`으로 바꾸자 M2 P@10이 0.0829 → 0.2327로 **2.8배** 올랐고 MFCC를 90% 앞선다.
  D-0024의 "MFCC가 MERT를 이긴다"는 모델 문제가 아니라 레이어 선택 문제였다.
  **기본 뷰는 `mert-layers` 저장소의 `layer00`이다.**
- **결합 실측은 D-0025.** MERT+MFCC 결합이 **M1에서 어느 단독보다 우월**하다(MAP@10 0.1022,
  MFCC 대비 +40%). 두 표현은 상보적이며, MERT를 버리는 판단은 틀린 것이었다.
  M2는 무승부이므로 MERT의 기여는 앨범 수준에 한정된다.
- **코퍼스 구조 도출**: 1004곡 중 595곡이 단일 곡 앨범이라 M1 쿼리는 409뿐이다.
  아티스트당 평균 약 17곡으로 **M2의 검정력이 M1보다 높다.**
- 실행: `uv run python -m hathor.cli eval retrieval --out var/ingest`

**한계**: 앨범·아티스트 검색은 취향의 대리 지표가 아니다. 임베딩이 곡 정체성과
제작 맥락을 담는지까지만 말해준다.

### □ 절제 실험

축 분리 설계가 실제로 작동하는지 검증한다.

| 조건 | 제거 축 | 확인 사항 |
|---|---|---|
| 전체 | 없음 | 기준선 |
| −음악 | 음악축 조건 제거 | 멜로디·화성 특성 변화 |
| −편곡 | 편곡축 조건 제거 | 악기·음색 변화 |
| −가사 | 가사축 조건 제거 | 주제·정서 변화 |
| −가창 | 가창축 조건 제거 | 창법·표현 변화 |

각 축을 제거했을 때 해당 특성만 변화하고 다른 축이 유지되면 축 분리가 유효하다.

### □ 재현성 검증

CI에서 동일 시드 2회 실행 후 산출물을 비교한다. UUID 등 본질적으로 매 실행 달라지는 필드는 비교에서 제외한다.

## 11. 개발 환경 및 배포 설계

### □ 개발 환경

| 항목 | 기준 |
|---|---|
| OS | Ubuntu 22.04 LTS (양 기기 통일) |
| Python | 3.12 (`.python-version` 핀 고정) |
| 패키지 | uv + `uv.lock` |
| 컨테이너 | Docker Engine + Compose |

**기기 구성**

| | 리전 | 광인사 |
|---|---|---|
| CPU | i5 | Intel Ultra 7 155U |
| RAM | 8GB (WSL 4.8GB) | 16GB |
| GPU | GTX 1660Ti 6GB(가용 4.8GB), CUDA 13.3 | 없음 |
| 역할 | **GPU 워커** | **주 개발기** |

라이브러리 루트는 기기마다 드라이브 문자가 다르므로 `HATHOR_LIBRARY_ROOT` 환경변수로 외부화한다.

**시스템 바이너리 버전** (실측 필요 — 통일 후 재기록)

| | 리전 | 광인사 |
|---|---|---|
| ffmpeg | 4.4.2 | 8.0.1 (통일 예정) |
| fpcalc | 1.5.1 | 미설치 |

### □ 컨테이너 구성

| 서비스 | 용도 |
|---|---|
| PostgreSQL + pgvector | 트랙·특징·취향 벡터 |
| MongoDB | 생성 이력·중간 산출물 |
| Redis | 작업 상태 |
| RabbitMQ | 생성 작업 큐 |
| MinIO | 산출물 오브젝트 스토리지 |

### □ CI 구성

| 잡 | 검사 |
|---|---|
| quality | ruff / ruff format / mypy --strict / import-linter / pytest + 커버리지 / pip-audit |
| secret-scan | gitleaks |
| reproducibility | 동일 시드 2회 실행 산출물 일치 |

## 12. 산출물 목록

| # | 산출물 | 형태 | 단계 |
|---|---|---|---|
| 1 | 기획서 (본 문서) | Markdown | P0 |
| 2 | 결정 기록 | `docs/DECISIONS.md` | 전 단계 |
| 3 | 인제스트 파이프라인 | 코드 + CLI | P1 |
| 4 | 코퍼스 실측 리포트 | JSON + 스크립트 | P1 |
| 5 | 4축 특징 추출 파이프라인 | 코드 | P2 |
| 5.1 | 검색 평가 하네스 + 리포트 | 코드 + JSON | P1 |
| 6 | 취향 모델 | 코드 + 학습 산출물 | P3 |
| 7 | 로컬 에이전트 | 데스크톱 앱 | P4 |
| 8 | 생성 파이프라인 | 코드 | P5~P7 |
| 9 | 안전 게이트 | 코드 + 캘리브레이션 리포트 | P7 |
| 10 | 평가 리포트 | 문서 + 지표 | P8 |
| 11 | 데모 모드 | 웹 | P8 |

## 13. 갱신 이력

| 판 | 일자 | 내용 |
|---|---|---|
| 1.0 | 2026-08-10 | 초판. MASTER.md를 대체하는 3부작 체계로 전환. 로컬 에이전트 아키텍처 확정 |
| 1.1 | 2026-08-16 | §10에 검색 평가 하네스 절 신설. REQ-EVL-000 추가. D-0023 반영 |
| 1.2 | 2026-08-16 | §10 지표 실측값 반영. O-8 등재. D-0024 반영 |
| 1.3 | 2026-08-16 | 결합 뷰 실측 반영. O-8 범위 축소. D-0025 반영 |
| 1.4 | 2026-08-16 | 레이어 실측 반영. 기본 뷰를 layer00으로 변경. O-8 해소, O-9 등재. D-0026 반영 |
| 1.5 | 2026-08-16 | 레이어 곡선 종결. O-9 해소, O-10(취향 라벨) 등재. D-0027 반영 |
| 1.6 | 2026-08-17 | 취향 라벨 수집 착수. D-0028 반영 |
| 1.7 | 2026-08-17 | 우선순위 정정 — 주기능은 시드곡 퓨전. O-10 격하, O-11 등재. D-0029 반영 |
| 1.8 | 2026-08-17 | 허브 곡 문제 발견, 중심화 도입. D-0030 반영 |
| 1.9 | 2026-08-17 | 중심화 실측 — 전 지표 개선. 기본값 승격. D-0031 반영 |
| 1.10 | 2026-08-17 | 레이어 순위 재확인. layer00 확정. D-0032 반영 |
| 1.11 | 2026-08-17 | 퓨전 결합 규칙 3종과 M4 지표 도입. D-0033 반영 |
| 1.12 | 2026-08-17 | 결합 규칙 실측 — MEAN 유지. M4 베이스라인 추가. D-0034 반영 |
| 1.13 | 2026-08-17 | 퓨전 축 종료. M4 해석 한계 명시. D-0035 반영 |
| 1.14 | 2026-08-17 | 가사축 착수. D-0036 반영 |
| 1.15 | 2026-08-17 | 가사축 M0 미달 진단. D-0037 반영 |
| 1.16 | 2026-08-17 | 가사축 개선 소진. O-12 등재. D-0038 반영 |
| 1.17 | 2026-08-17 | 문서 3축 체계 명시. O-13 등재. D-0039 반영 |
| 1.18 | 2026-08-17 | M0 분할을 실험 축으로 승격. 순위 진단·베이스라인 추가. O-12 갱신. D-0040 · D-0041 반영 |
| 1.19 | 2026-08-17 | 문서 정비. 부록 A 손요약 폐지·기계 색인 전환, 항법 표기, README 감량. O-14 등재. D-0042 반영 |
| 1.20 | 2026-08-17 | `PLAN.md` → `DESIGN.md`. GROUND RULES 회수 — GR-0·1·4·5·6은 `CONTRIBUTING.md`, GR-2는 §1 표기, GR-3은 §2 흡수. D-0043 반영 |
| 1.21 | 2026-08-17 | O-12 판정 완료. M0 게이트 전제 반증(O-15 등재), 미달 진단 문구 정정. D-0044 반영 |
| 1.22 | 2026-08-17 | 가사 언어 구성 실측(혼재 40%). BGE-M3 인코더 채택, 판정표 사전 고정. O-16 등재. D-0045 반영 |
| 1.23 | 2026-08-17 | BGE-M3 실측(M1 4.0배·M2 3.4배, 5배 미달). 판정 보류, 평균 풀링 추가. 이방성 0.7645 실측. D-0046 반영 |
| 1.24 | 2026-08-18 | CLI 배선 테스트 추가. `--pooling`이 파서에 없던 결함 수정. D-0047 반영 |
| 1.25 | 2026-08-18 | 가사축 종료(풀링 무관, M1 4.2배). 구조 생성 착수 — 가사 반복 패턴 기반. O-17·O-18·O-19 등재. D-0048 · D-0049 반영 |
| 1.26 | 2026-08-18 | 구간 분할 폴백 순서 수정(O-18 해결, 1004곡 복구). 구조 임계 민감도 실측 — 절대 임계 기각, 상대 기준으로. D-0050 · D-0051 반영 |
| 1.27 | 2026-08-18 | **생성 경로 종단간 관통.** 구조 → 화성 → 배치 → MIDI. n=1004 재추출로 축 간 베이스라인 정렬. O-20 등재. D-0052 · D-0053 반영 |
| 1.28 | 2026-08-18 | 참조곡 조성 추정(K-S, 의존성 없음). 참조곡 상한 강제. O-21·O-22 등재. D-0054 반영 |
| 1.29 | 2026-08-18 | 조성 진단 3종(교차표·무작위 베이스라인·나란한조 비율). **격차 지표가 무작위와 구분되지 않음을 확인.** D-0055 반영 |
| 1.30 | 2026-08-18 | **크로마를 반음 격자(CQ 유사)로.** 선형 FFT 저역 결함 확정 — 합성 60음 오판 7→0. `linear`는 베이스라인으로 보존. D-0056 반영 |
| 1.31 | 2026-08-18 | 반음 격자 실측 — D 편중 29%→10.5%, 격차 판별력 부활. **예측 오류와 지표 분모 오류를 기록.** 조율 편차 측정 추가. O-23 등재. D-0057 반영 |
| 1.32 | 2026-08-18 | 조율 가설 기각(+8센트). **로그 압축 반증** — 상관은 균등 성분에 불변이다. Temperley 프로파일 추가. O-24 등재. D-0058 반영 |
| 1.33 | 2026-08-18 | **베이스라인이 프로파일을 안 따라가 판정이 뒤집혀 있었다.** 프로파일 가설 기각. 배음 감산 추가, 크로마 저장으로 음원 없이 재판정 가능. D-0059 반영 |
| 1.34 | 2026-08-18 | 베이스라인이 배음도 안 따라감(여섯 번째). **검은건반 비율을 아예 안 찍고 있었다.** 배음 스윕 도구화. O-25 등재. D-0060 반영 |
| 1.35 | 2026-08-19 | **O-23 기각 — 33.5%는 균등(41.7%)보다 낮았다.** 조성 추정 종료. 광인사 `make check` 상시 실패 수정. D-0061 반영 |

---

## 부록 A. 결정 기록 색인

**요약이 아니라 색인이다.** 제목만 옮기며 `tools/sync_decision_index.py`가 생성한다
(D-0042). 판단의 배경·후보·근거·한계는 `docs/DECISIONS.md` 본문에만 있고,
여기서 읽고 판단하지 않는다. 손요약을 두면 반드시 어긋난다 — 실제로 어긋났다.

<!-- decision-index:begin -->
<!-- 이 표는 tools/sync_decision_index.py가 생성한다. 손으로 고치지 않는다 (D-0042). -->

| ID | 결정 |
|---|---|
| D-0001 | MongoDB를 core 프로파일에 유지한다 |
| D-0002 | 본인 보컬 시드 녹음을 P7까지 연기한다 |
| D-0003 | 실존 가수의 음색 복제·보간은 구현하지 않는다 |
| D-0004 | 저장소를 WSL ext4에 두고 F 드라이브는 데이터 전용으로 쓴다 |
| D-0005 | 가사는 태그 우선, STT 폴백 경로를 유지한다 |
| D-0006 | ISRC 부재로 Chromaprint 지문 정규화가 주 경로가 된다 |
| D-0007 | 샘플레이트는 인제스트에서 통일하지 않는다 |
| D-0008 | MASTER.md는 단일 진실 공급원이되 코드가 충돌 시 우선한다 |
| D-0009 | 노트북 2대 역할을 재정의하고 파이썬 3.12로 고정한다 |
| D-0010 | ID3 전수 조사 결과 유효 태그는 4종이며 태그 기반 메타데이터 경로를 폐기한다 |
| D-0011 | 취향은 선호도 예측이 아니라 조건부 생성으로 다룬다 |
| D-0012 | 취향 웜스타트는 적응적 쌍대비교로 수행한다 |
| D-0013 | 라이브러리 변경 추적은 명시적 재스캔 + 델타 감지로 한다 |
| D-0014 | 아티스트 파서는 정규화하지 않고 후보를 제시한다 |
| D-0015 | 로컬 에이전트 아키텍처를 채택하고 오디오 비이동을 불변 원칙으로 둔다 |
| D-0016 | 아티스트 파서 출력 스키마와 표기 체계 휴리스틱 |
| D-0017 | 정규화 기준을 ISRC에서 MusicBrainz MBID로 옮기고 Track을 폐기한다 |
| D-0018 | R-1 해소 — GPU 실동작 확인, MERT 청크 10초 확정 |
| D-0019 | MusicBrainz 조회는 2단계 구조로 하고 78.5%를 상한으로 받아들인다 |
| D-0020 | 재생시간으로 레코딩 버전을 확정한다 — O-5 해소, D-0019 정정 |
| D-0021 | 오디오 디코딩은 ffmpeg 서브프로세스, 정규화는 MERT 특징 추출기에 맡긴다 |
| D-0022 | 배치 중복 실행은 flock으로 막고 인덱스는 키 정렬로 유지한다 |
| D-0023 | 검색 평가 하네스를 태그 동치류로 구성한다 (M0/M1/M2) |
| D-0024 | MERT-95M 혼합 임베딩이 MFCC 베이스라인에 진다 — 실측 9조건 |
| D-0025 | MERT와 MFCC는 상보적이다 — O-8 가설 (c) 약화, (a)·(b)로 좁힌다 |
| D-0026 | MERT 마지막 레이어가 최악이었다 — layer00으로 교체, M2 2.8배 개선 |
| D-0027 | 레이어 곡선은 layer00에서 최대다 — O-9 해소, 임베딩 축 종료 |
| D-0028 | 취향 라벨 수집은 무작위 평가 집합을 먼저 확보한다 |
| D-0029 | 주기능은 시드곡 퓨전이다 — 취향 모델은 보조로 격하 |
| D-0030 | 임베딩 공간이 뭉쳐 허브 곡이 생긴다 — 중심화 도입 |
| D-0031 | 중심화가 전 지표를 개선한다 — 기본값으로 승격 |
| D-0032 | 중심화 후에도 레이어 순위가 유지된다 — layer00 확정 |
| D-0033 | 시드 결합 규칙을 실측으로 고른다 — M4 신설 |
| D-0034 | MEAN을 유지한다 — MIN은 균형을 개선하지 않았다. M4에 베이스라인 추가 |
| D-0035 | 퓨전은 작동하나 M4를 최적화 목표로 쓰지 않는다 — 퓨전 축 종료 |
| D-0036 | 가사축은 문자 n-gram 해싱 베이스라인으로 착수한다 |
| D-0037 | 가사축 M0 미달 — 진단 결과와 개선 축 3종 |
| D-0038 | 가사축 개선 3종 소진 — M0 정의가 가사에 맞지 않는다는 가설 |
| D-0039 | 문서 체계를 3축으로 명시한다 — 암묵 구조를 선언으로 전환 |
| D-0040 | M0 분할을 실험 축으로 승격한다 — 홀짝의 결정성이 가사에서 위험하다 |
| D-0041 | M0에 순위 진단과 해석적 베이스라인을 넣는다 |
| D-0042 | 부록 A를 손요약에서 기계 색인으로 바꾸고 문서 검사를 CI에 넣는다 |
| D-0043 | `PLAN.md`를 `DESIGN.md`로, GROUND RULES를 `CONTRIBUTING.md`로 — 4종 재분배 |
| D-0044 | O-12 판정 — 분할 정렬은 실재하나 작다. 가사 표현이 한계이며 신경망 인코더로 간다 |
| D-0045 | 가사축 인코더를 BGE-M3로 한다 — 혼재 40%가 결정 근거이며 판정표를 먼저 박는다 |
| D-0046 | BGE-M3 실측 — 방향은 맞았으나 판정 보류. 5배 기준에는 근거가 없었다 |
| D-0047 | CLI 배선을 테스트로 고정한다 — 인코더 테스트는 배선을 보지 않는다 |
| D-0048 | 가사축을 여기서 종료한다 — 풀링이 병목이 아니었다 |
| D-0049 | 구조 생성을 가사 반복 패턴에서 규칙으로 뽑는다 |
| D-0050 | 가사 구간 폴백 판정을 정리 뒤로 옮긴다 — 제외 1곡의 원인 확정 |
| D-0051 | 구조 추출의 절대 임계는 결과를 지배한다 — 상대 기준이 필요하다 |
| D-0052 | 생성 경로가 종단간 관통했다 — MIDI를 직접 쓴다 |
| D-0053 | n=1004 재추출 — 베이스라인이 축마다 달랐다 |
| D-0054 | 참조곡에서 조성을 추정한다 — 시드 퓨전이 구조에만 걸려 있었다 |
| D-0055 | 조성 추정 진단 — 격차 지표는 무작위와 구분되지 않는다 |
| D-0056 | 크로마를 반음 격자로 바꾼다 — 선형 FFT는 저역에서 반음을 못 가른다 |
| D-0057 | 반음 격자 실측 — 예측 하나가 틀렸고, 진단 지표의 분모가 틀렸다 |
| D-0058 | 로그 압축은 처방이 아니었다 — 평평함은 증상이지 원인이 아니다 |
| D-0059 | 베이스라인이 조건을 따라가지 않았다 — 판정이 뒤집혔다 |
| D-0060 | 베이스라인이 또 조건을 안 따라갔다 — 그리고 핵심 지표를 안 찍고 있었다 |
| D-0061 | O-23을 기각한다 — 쫓던 이상 자체가 베이스라인 없는 판정이었다 |
| D-0062 | O-21의 판정 장치를 생성기보다 먼저 만든다 — 조건화가 질 수 없는 지표를 피한다 |
| D-0063 | O-21을 닫는다 — 참조곡이 화성 어휘에 반영된다. 크기는 K-K 대비의 14%다 |
| D-0064 | 창별 중앙값 크로마를 넣는다 (O-27 (b)) — 그리고 `harmonic`이 조용히 무시되고 있었다 |
| D-0065 | O-27 (b) 기각 · 그리고 "귀무 λ\*는 0이어야 한다"가 틀린 기대였다 |
| D-0066 | 두 기기 개발 환경을 규약으로 고정한다 — 경로를 손으로 치지 않는다 |
| D-0067 | 규약을 기계가 확인하게 한다 — 규칙이 없어서가 아니라 확인하는 것이 없어서였다 |
| D-0068 | 기기 설정을 탐지해서 쓴다 — 탐지 가능한 것을 손으로 치게 했다 |
| D-0069 | 부류를 검사로 바꾼다 — 같은 결함이 네 시간 만에 재발했다 |
| D-0070 | 패치 적용과 커밋을 한 명령으로 묶는다 — 손으로 만든 명령이 매번 틀렸다 |
| D-0071 | 검사가 기기 상태에 의존했다 — 검사가 잡아야 할 부류를 검사 자신이 저질렀다 |
| D-0072 | 커밋 대상을 패치와 대조한다 — `git add -A`는 트리를 믿는 것이었다 |
| D-0073 | O-27 (a) 타악 분리를 구현한다 — 그리고 산출물에 조건을 적는다 |
| D-0074 | O-27 (a) 통과 — 드럼을 빼니 대비가 30%에서 87%로 올랐다. O-21을 다시 연다 |
| D-0075 | 조성 추출을 이어받게 한다 — 1004곡 배치가 원자적이었다 |
| D-0076 | (결번) 기록이 유실됐다 — 무엇이었는지 복원할 수 없다 |
| D-0077 | 추출을 동시에 못 돌게 한다 — 이어받기가 만든 결함이다 |
| D-0078 | 전량 배치를 마쳤다 — 출력 차이는 8마디로 판정할 수 없다 |
| D-0079 | O-29 도구를 만든다 — 8마디 실측은 극한보다 크고, 그 초과분은 정보가 아니다 |
| D-0080 | 결정 기록 자신의 규약을 기계가 검사하게 한다 — 78건이 쌓이는 동안 아무도 안 봤다 |
| D-0081 | 표기는 전수 강제하고 내용만 신규에 건다 — D-0080에서 둘을 뭉뚱그렸다 |
| D-0082 | O-29를 닫는다 — 전달된다. 그리고 "3배가 안 왔다"는 단위 착각이었다 |
| D-0083 | O-31 도구 — 버리는 칸의 몫은 나눗셈 하나로 갈린다. 모형이 필요 없었다 |
| D-0084 | 귀무선이 질량까지 바꿨다 — D-0062가 경고한 부류에 다시 빠졌다 |
| D-0085 | O-31을 닫는다 — 버린다. 그리고 버려지는 칸 중 하나는 반음계음이 아니었다 |
| D-0086 | 귀무선의 0점이 0이 아니었다 — 같은 자리에서 세 번째다 |
| D-0087 | 선을 하나 더했더니 다른 선의 수가 바뀌었다 — 귀무선 추출의 결함 |

<!-- decision-index:end -->

## 부록 B. 용어

| 용어 | 정의 |
|---|---|
| 4축 | 음악·편곡·가사·가창. 취향과 생성을 분해하는 기본 단위 |
| 취향 벡터 | 축별 잠재 공간 위에서 사용자의 선호를 나타내는 벡터 |
| 시드곡 | 생성 시 참조로 지정하는 곡. 축별 독립 지정 가능 |
| 스템 | 곡을 악기별로 분리한 오디오 트랙 |
| 델타 감지 | 이전 스캔 대비 변경분만 식별하는 처리 |
| 오디오 비이동 원칙 | 원본 오디오가 사용자 기기를 떠나지 않는다는 설계 불변 조건 |
| SV | 화자 검증(Speaker Verification). 음색 동일성 판정에 사용 |
| 실측 필요 | 검증되지 않아 추측으로 채우지 않은 항목 (GR-0.5) |
